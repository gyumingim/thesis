# (가제) 동일 연산 예산에서의 경량 시뮬레이터 기반 자율주행 강화학습

> 상태: **초안 골격**. 확정된 것과 미정인 것을 구분해서 표기한다.
> 미정 항목에 임의의 값이나 주장을 채워 넣지 않는다.

---

## 0. 확정 / 미정

| 항목 | 상태 |
|---|---|
| 논문 주장 = 동일 wall-clock 비교 | ✅ 확정 (2026-08-18) |
| 고충실도 비교군 = MetaDrive | 🟡 제안, 승인 대기 |
| 교차 평가 추가 | 🟡 제안, 승인 대기 |
| 시뮬 구현 스택 | 🟡 Python+NumPy, Numba는 벤치 후 채택 |
| 환경 API = Gymnasium VectorEnv | ✅ 확정 (2026-08-18) |
| RL = PPO, 기존 구현 재사용 | ✅ 확정 (2026-08-18) |
| PPO 구현체 = CleanRL `ppo_continuous_action.py` | ✅ 확정 (연속제어 확정에 따라) |
| 메인 과제 = intersection | ✅ 확정 (2026-08-18) |
| 일반화 검증 과제 = bottleneck (2차) | ✅ 확정 (2026-08-18) |
| 행동공간 = 연속 | ✅ 확정. 단 **3차원(steering/throttle/brake) → 2차원으로 정정** (아래 3.1) |
| 관측 = 저차원 벡터 (카메라 없음) | ✅ 확정. 단 **객체리스트 → 라이다 광선으로 정정** (아래 3.1) |
| 마이크로벤치 실행 | 🟡 승인 대기 |
| 주행 과제 | ❌ 미정 |

---

## 1. 문제 제기

자율주행 강화학습은 수억~수십억 스텝의 경험을 요구한다. 고충실도 시뮬레이터는
스텝당 비용이 커서, 연구자가 실제로 쓸 수 있는 샘플 수가 시뮬레이터의 처리량에
의해 결정된다. 그러나 기존 비교는 대부분 **동일 샘플 수** 기준이며,
연구자가 실제로 마주하는 제약인 **동일 연산 시간** 기준의 비교는 드물다.

핵심 질문:

> 같은 wall-clock 예산에서, 물리를 추상화한 경량 시뮬레이터의 대량 샘플이
> 고충실도 시뮬레이터의 소량 샘플을 이기는가? **어디까지 이기고, 어디서 깨지는가?**

## 2. 선행연구 (1차 출처 확인 완료)

| 연구 | 처리량 | 제약 | 본 연구와의 관계 |
|---|---|---|---|
| GPUDrive (Emerge-Lab) | 1M FPS | Madrona C++/CUDA 빌드, Waymo 데이터셋 필요 | 처리량 상한의 기준점. 본 연구 하드웨어에서는 실행 불가(데이터셋·RAM) |
| Waymax (Waymo) | JAX, 가속기 전체 실행 | Waymo Open Motion Dataset 필요 | 데이터셋 의존 경로 |
| HighJax (HumanCompatibleAI) | JAX, JIT/vmap | 이산 메타행동, 하이웨이 한정 | **가장 가까운 선행연구.** 경량 JAX 주행 시뮬 자체는 이미 존재 |
| MetaDrive | 1000~1500 FPS (문헌값, 본 장비 실측 필요) | 파이썬/Panda3D 단일 프로세스 | 고충실도 비교군 후보 |
| highway-env (Farama) | 수백~수천 | 순수 파이썬 | 경량 2D의 기존 표준 |

**본 연구의 위치:** 새 시뮬레이터를 제안하는 것이 기여가 아니다(위 표대로 선점됨).
기여는 **동일 연산 예산 하의 통제된 비교와, 추상화가 깨지는 지점의 규명**이다.

## 3. 실험 설계 (제안)

### 3.1 통제 조건

비교가 성립하려면 아래가 동일해야 한다:

- 주행 과제 (미정)
- 관측 공간 — MetaDrive `StateObservation` 구조를 경량 시뮬이 복제:
  - ego 6차원: 좌/우 도로경계 거리, 차선 대비 헤딩차, 정규화 속도, 조향, 직전 행동, yaw rate
  - navi 10차원: 체크포인트 2개 × (진행방향 투영거리, 측방 투영거리, 곡률반경, 회전방향, 차선각)
  - 라이다 광선 거리
  - **관측 방식 = GT 객체 리스트 (사용자 결정, 2026-08-18).** 광선은 한 개도 쏘지 않는다.
    **정정(실측):** 설정만으로는 불가능하다. `LidarStateObservation.lidar_observe()` 가 GT 주변차
    정보를 `num_lasers > 0` 블록 안에 두었기 때문에, `num_lasers=0` 은 광선과 GT를 **함께** 끈다
    (실측: obs 19차원 = ego 9 + navi 10, GT 32 누락).
    따라서 `agent_observation` 훅(`base_env.py:672`)으로 `GTStateObservation` 을 주입해
    게이트를 우회한다. 주변차 탐지는 원래도 broad-phase `contactTest`(`lidar.py:169`)라 광선과 무관하다.
    **검증:** 광선 0개 경로와 광선 1개 경로의 GT 32값이 146스텝 동안 최대차이 0.000e+00 으로 일치.
    정규화 공식(`lidar.py:110`): `clip((rel_pos / perceive_distance + 1) / 2, 0, 1)`
  - 이 결정으로 **"센서 시뮬레이션까지 경량화"라는 기여 주장은 포기한다.**
    양쪽 모두 광선을 쏘지 않으므로 기여 범위가 물리·환경 표현의 경량화로 좁아진다.
    광선 캐스팅 비교는 후속 확장으로 남긴다.
  - 근거: 빠른 주행 시뮬의 사실상 표준이 GT 객체 리스트다.
    HighJax 관측 = `(presence, x, y, vx, vy)`, GPUDrive도 객체 상태 기반.
  - 신호등은 1차 실험에서 제외 (비교군 기본 설정에 없는 정보를 한쪽만 갖게 됨)
- 행동 공간 — MetaDrive `env_input_policy.py:62`가 `gym.spaces.Box(-1.0, 1.0, shape=(2,))`.
  `action[0]=steering`, `action[1]=throttle_brake`(음수=제동, `base_vehicle.py:472`).
  **스로틀/브레이크를 별도 축으로 두면 비교군과 행동공간이 달라져 비교가 무효가 되고,
  throttle=1 & brake=1 이라는 정의되지 않은 입력이 생긴다. 반드시 2차원으로 맞춘다.**
- 종료조건 8종 (`metadrive_env.py:133`): CRASH_VEHICLE / CRASH_OBJECT / CRASH_BUILDING /
  CRASH_HUMAN / CRASH_SIDEWALK / OUT_OF_ROAD / SUCCESS / MAX_STEP
- 보상 상수 (`metadrive_env.py:72`): success_reward=10.0, out_of_road_penalty=5.0,
  crash_vehicle_penalty=5.0, crash_object_penalty=5.0, crash_sidewalk_penalty=0.0
- RL 알고리즘 구현 — **양쪽 시뮬에 완전히 동일한 CleanRL PPO 단일 파일**.
  래퍼 스택(`NormalizeObservation`/`NormalizeReward` 등)까지 동일해야 함
- **양쪽 모두 벡터화** — 경량 시뮬만 병렬화하고 비교군을 단일 환경으로 두면 비교 무효.
  MetaDrive도 `AsyncVectorEnv`로 16스레드 전부 사용한 상태의 실측치를 기준선으로 삼는다
- 랜덤 시드 개수
- **wall-clock 예산** (예: GPU-시간 N시간, 동일 하드웨어)

### 3.2 학습·평가 행렬

|  | 경량 시뮬에서 평가 | 고충실도 시뮬에서 평가 |
|---|---|---|
| **경량 시뮬에서 학습** | (내부 상한) | **← 핵심 셀** |
| **고충실도 시뮬에서 학습** | (역방향 전이) | (기준선) |

핵심 셀이 이 논문의 결과다. 나머지 셀은 그것을 해석하기 위한 통제군이다.

### 3.3 보고할 지표

- wall-clock 대비 성능 곡선 (샘플 수가 아니라 **시간**을 x축으로)
- 역전 시점: 경량 쪽이 고충실도 쪽을 넘어서는 시각 (존재한다면)
- 실패 모드 분류: 경량 정책이 고충실도에서 깨지는 원인
  (후보: 타이어 슬립, 관성, 센서 노이즈, 교통 참여자 반응성 — 미확정)

### 3.4 MetaDrive 실측 파라미터 (소스 확인 완료, 우리 시뮬이 복제할 값)

| 항목 | 값 | 출처 |
|---|---|---|
| 제어 주기 dt | 0.1 s (`physics_world_step_size=2e-2` × `decision_repeat=5`) | `base_env.py:190-191` |
| 물리 주기 | 0.02 s (50 Hz) | 동일 |
| 에피소드 최대 길이 | 1000 스텝 (100초) | `metadrive_env.py:58` |
| GT 감지 반경 | 50 m | `base_env.py:170` |
| 관측 차량 수 `num_others` | **미정 — 결정 필요** (기본 0) | `base_env.py:170` |
| 교통 밀도 / 모드 | 0.1 / `TrafficMode.Trigger` | `metadrive_env.py:37,39` |
| 행동공간 | `Box(-1, 1, shape=(2,))` | `env_input_policy.py:62` |
| (미사용) LiDAR | 240개, 50 m, 360° 균등 (`2π/num_lasers`, offset 0) | `base_env.py:170`, `distance_detector.py:177-179` |

**미검증 항목:** `num_lasers=0` + `num_others=N` 조합이 실제로 동작하는지는 설치 후 확인해야 한다.
`_get_lidar_range()`가 `num_lasers=0`에서 `None`을 반환하는 경로가 있어 호출부 가드에 의존한다.
소스만 보고 단정하지 않는다.

### 3.5 기준선 실측 (RTX 4060 Laptop / Ryzen 7 8845HS, 단일 환경)

교통 밀도 0.4, map="X", 3회 반복 평균 ± 표준편차:

| 구성 | obs | SPS |
|---|---|---|
| GT only (광선 0) | 51 | **679.8 ± 6.0** |
| GT + 광선 1 | 52 | 661.2 ± 16.2 |
| GT + 광선 240 | 291 | 640.9 ± 2.8 |
| 광선 240만 (GT 0) | 259 | 651.0 ± 3.6 |

교통 밀도 0.1(MetaDrive 기본), GT + 광선 1 구성: **1532.0 ± 5.0 SPS**

**읽어낸 것**
1. **광선 캐스팅은 비용의 5.7~6.4% 에 불과하다** (밀도 0.4에서 679.8→640.9, 밀도 0.1에서 1532→1434).
   따라서 경량 시뮬의 처리량 우위는 센서 제거에서 오지 않는다. 물리엔진(Bullet)·씬그래프·
   파이썬 객체 오버헤드를 제거하는 데서 와야 한다.
2. **GT 8대 추가는 사실상 공짜다** (1434.4 vs 1433.3, 노이즈 내). broad-phase 가 어차피 돌기 때문.
3. **교통 밀도가 광선보다 훨씬 큰 요인이다** (0.1→0.4 에서 1532→661, 2.3배). 밀도를 고정해 보고해야 한다.

### 3.6 재현 환경 주의사항

- ROS 2 Humble 의 `PYTHONPATH` 가 venv 로 새어 들어온다. 모든 측정은 `env -u PYTHONPATH` 로 격리한다.
- MetaDrive 0.4.3 의 `horizon` 기본값은 `None` 이다 (GitHub main 의 `1000` 과 다르다). 명시 지정한다.
- MetaDrive 0.4.3 `BaseEnv.reset(self, seed=None)` 은 `options` 인자를 받지 않아
  gymnasium 1.2.3 `AsyncVectorEnv` 와 직접 호환되지 않는다. 래퍼로 흡수한다.
- MetaDrive 는 전역 엔진 싱글턴이라 한 프로세스에 두 환경을 동시에 띄울 수 없다.

### 3.7 경량 시뮬 커널 마이크로벤치 (Table 1 초안)

세 구현(NumPy / Numba / PyTorch-GPU)이 **동일 연산**임을 먼저 검증했다
(`bench/test_equivalence.py`: 무작위 상태 10회, obs 최대차이 2.98e-07, reward·done 완전 일치).
검증 전 실측에서 NumPy `argpartition` 의 비정렬성이 관측 슬롯 불일치(최대 1.84)를 냈고,
거리순 정렬로 수정 후 동치가 됐다 — 동치 검증 없이 SPS 만 쟀다면 서로 다른 관측을 비교할 뻔했다.

SPS (환경 수 × 스텝 / 벽시계 초), 구성당 1.5초 예산, 워밍업 30스텝(JIT·CUDA 캐시 제외):

**V=4 (차량 4대)**
| E | NumPy | Numba(8T) | Torch(GPU) |
|---|---|---|---|
| 1 | 3,135 | 173,193 | 803 |
| 64 | 196,148 | 3,281,967 | 46,968 |
| 256 | 377,161 | 5,507,512 | 168,629 |
| 1024 | 529,525 | 10,770,320 | 627,277 |
| 4096 | 524,944 | **15,086,516** | 2,512,121 |

**V=16**
| E | NumPy | Numba(8T) | Torch(GPU) |
|---|---|---|---|
| 1 | 4,622 | 190,414 | 778 |
| 64 | 55,120 | 1,796,940 | 44,820 |
| 256 | 66,666 | 2,905,568 | 163,983 |
| 1024 | 55,524 | **4,147,402** | 455,202 |
| 4096 | 63,247 | 3,540,819 | 612,659 |

**V=64**
| E | NumPy | Numba(8T) | Torch(GPU) |
|---|---|---|---|
| 1 | 2,509 | 90,276 | 727 |
| 64 | 5,150 | 531,119 | 36,966 |
| 256 | 4,742 | 636,241 | 148,979 |
| 1024 | 4,919 | 654,962 | 602,295 |
| 4096 | 5,076 | 636,681 | **847,177** |

**읽어낸 것**
1. **Numba 채택.** V=16, E=1024 에서 4.1M SPS — MetaDrive 단일환경(1,532 SPS) 대비 약 2,700배.
   "연산량 압도" 전제가 실측으로 성립한다.
2. **스레드 수 함정.** Ryzen 7 8845HS 는 물리 8코어/SMT 16스레드인데, OMP 16스레드는
   SMT 경합으로 8스레드 대비 103배 느려진다(0.059→6.128 ms/step). `NUMBA_NUM_THREADS=8` 고정.
   이 사실 자체가 재현성 절에 들어가야 한다.
3. NumPy 는 O(V²) 쌍거리에서 무너진다 (V=64 에서 5K SPS). Numba 도 V=64 에서 0.6M 으로 내려앉지만
   Torch(GPU) 는 V·E 가 커질수록 상대적으로 유리해져 V=64, E=4096 에서 처음 Numba 를 이긴다.
   과제 설정(V=16 내외)에서는 Numba 가 명백한 승자다.
4. Torch 는 E=1 에서 803 SPS — 커널 런치 오버헤드가 지배. GPU 는 대량 배치에서만 의미가 있다.

### 3.8 MetaDrive 병렬 기준선 (공정 비교의 분모, 2026-08-20 실측)

AsyncVectorEnv, GT 관측, 밀도 0.1, 프로세스당 300스텝 × 2회 평균:

| 프로세스 | 총 SPS | 비고 |
|---|---:|---|
| 1 | 1,284 | IPC 오버헤드로 단일(1,532)보다 낮음 |
| 2 | 2,369 | |
| 4 | 4,097 | 거의 선형 |
| 8 | 5,687 | |
| 10 | 6,515 | |
| **12** | **6,965** | **정점** |
| 14 | 5,815 | |
| 16 | 5,745 | SMT 경합 — Numba 때와 동일 패턴 |

**공정 비교 확정치: 경량(Numba, V=16, E=1024) 4,147,402 ÷ MetaDrive 병렬 정점 6,965 ≈ 595배.**
논문 헤드라인은 "단일환경 대비 2,700배"가 아니라 **"동일 하드웨어를 최대로 쓴 기준선 대비 ~600배"** 로 쓴다.
SMT 경합이 비교군에서도 재현된 것(12>16)은 재현성 절의 근거가 된다.

**확정 결정 (2026-08-20):** 과제 차량 수 V≤16 고정, 시뮬 구현은 **Numba 단독 채택**.
V=64/E=4096 에서의 GPU 역전은 분석 절에 관찰로만 언급한다.

**미완(다음 단계):** 환경 완성(관측 51차원 의미론·밀집 보상·차선 기하 — 3.7 갭 2),
CleanRL PPO 연결, 동일 wall-clock 학습 곡선.

### 3.9 환경 완성 및 완성 환경 SPS (2026-08-20)

경량 시뮬을 벤치 커널에서 **완전한 RL 환경**으로 완성했다 (`bench/env_numba.py`).

**MetaDrive 와 정합된 것 (모든 공식은 설치된 0.4.3 소스 라인 확인)**
- 관측 51차원: ego 9 + navi 10(체크포인트 2개×5) + GT 8대×4 — 정규화 공식 동일
  (GT 속도는 km/h 상대속도를 max_speed 80 으로 정규화 — `lidar.py:116`)
- 보상: `1.0×Δ진행거리 + 0.1×(속도km/h / 80)`, 종료 시 +10/−5/−5 로 **덮어쓰기** (`metadrive_env.py:245`)
- 행동 `Box(-1,1,(2,))`, dt 0.1s(0.02×5), horizon 1000, 감지반경 50m

**의도적 근사 (논문 명시)**: 자전거 모델(WB 2.8m), 원형 충돌(r 2.5m), 십자영역 이탈판정,
순수추종+간격법 NPC, 편도 1차선 맵. MetaDrive 는 각각 Bullet 강체/박스/on_lane/IDM+MOBIL/다차선.

**행동 검증 (`bench/test_env.py`)**
- 스크립트 전문가(순수추종): 65 에피소드 중 63 도달·이탈 0, 평균 리턴 134.4
  (기대값 = 경로 123~128 + 속도보상 ~10 + 도달 10 과 부합 → 보상·기하·종료가 맞물려 동작)
- 풀조향 → 이탈 272회, 직진 강행 → 충돌 781회, 무작위 500스텝 obs∈[0,1]·NaN 없음

**완성 환경 처리량 (E=1024, V=16, 8스레드)**
| | SPS | MetaDrive 병렬 정점(6,965) 대비 |
|---|---:|---:|
| 벤치 커널 (40차원 관측, 3.7절) | 4,147,402 | 595배 |
| **완성 환경 (51차원 + 경로추적 + NPC 순수추종)** | **1,342,446** | **193배** |

예상대로 완성 비용(관측 조립·경로 추적·NPC 제어)이 커널 대비 3.1배를 소모했지만,
공정 비교 기준 **193배**가 유지된다. 이것이 논문의 확정 처리량 주장이다.

### 3.10 PPO 연결 및 학습 확인 (2026-08-20)

**동일 PPO 원칙의 구현** — `bench/ppo.py` 는 CleanRL `ppo_continuous_action.py`
(커밋 `fe8d8a0`, `bench/cleanrl_ppo_commit.txt` 에 고정)의 최소 수정판이다. 수정 3곳:
env 생성부(`--sim custom|metadrive` 분기, 동일 래퍼 스택), 에피소드 로깅(gymnasium 1.x
벡터 API), Args 필드 추가. **GAE·업데이트 루프·네트워크·하이퍼파라미터는 원본 그대로**이며
diff 로 증명 가능하다 (`cleanrl_ppo_continuous_action_orig.py` 보존).

**리셋 의미론 통일** — gymnasium 1.2.3 의 stateful 벡터 래퍼(NormalizeObservation 등)가
SAME_STEP 을 지원하지 않아(vector_env.py:542 assert) 커널을 표준 NEXT_STEP 으로 전환.
MetaDrive 쪽 AsyncVectorEnv 기본값과 동일 → 양쪽 의미론 일치. 전환 후 검증 5종 재통과.

**환경 수정 1건** — NPC 가 ego 스폰 지점 위에 태어나 length=1 에피소드가 생기는 문제를
발견(래퍼 검사 중 즉사 에피소드로 표면화), 스폰 시 ego 반경 8m 회피(5회 재시도)로 수정.
수정 후 전문가 평균 리턴 134.4→136.2.

**학습 스모크런 (custom, 64env×64step, 20만 스텝, 기본 하이퍼파라미터)**
| 진행 | 평균 에피소드 리턴 |
|---|---:|
| 초반 10개 | 4.0 |
| 40~60% | 64.8 |
| 마지막 10개 | **71.8** |

전문가(순수추종) 136, 무작위 ≈ −5~0 기준 — **PPO 가 우리 환경에서 실제로 주행을 학습한다.**
에피소드 길이 48→91 (더 오래 생존). MetaDrive 갈래도 동일 파일로 end-to-end 구동 확인.

**중요한 관찰 — 본실험 설계에 반영할 것:**
학습 포함 SPS 는 custom 6,030 / MetaDrive 179 (각각 64env/4env 소규모 기준).
custom 쪽은 env 가 아니라 **PPO 업데이트가 병목**이다 (env 1.35M SPS vs 학습 6k).
따라서 wall-clock 우위는 num_envs 를 키워 학습 배치를 키울 때 실현된다 —
"싼 시뮬 = 대량 num_envs 가능"이 곧 논문의 메커니즘이다. 본실험은
custom(num_envs 1024~2048) vs MetaDrive(num_envs 12 = 병렬 정점)로,
**같은 하드웨어에서 각자의 최적 구성**을 쓰는 동일 wall-clock 비교로 설계한다.

### 3.11 파일럿 실험 (2026-08-20, 30분×2시드×2시뮬)

**수집량 (동일 30분, 학습 포함):** custom 1억 2,800만~1억 3,200만 스텝 (SPS ~72k) vs
MetaDrive 245만 스텝 (SPS ~1,360) — **53배**. custom 은 num_envs=1024 에서 GPU 배치 효율로
학습 SPS 가 64env 대비 12배 상승 (6k→72k). 3.10 의 예측 그대로.

**평가 행렬 (최종 체크포인트, 결정론 정책, 평가시드 분리):**

| 학습\평가 | custom | MetaDrive |
|---|---|---|
| custom | 성공 14% / 충돌 86% / **이탈 0%** / R 69.7 | 성공 0% / **이탈 87~93%** |
| MetaDrive | (미측정) | 성공 50% / 충돌 27% / 이탈 23% / R 94.9 |

**진단 — 전이 0% 는 도메인 갭이 아니라 계측 버그였다.**
in-domain 이탈 0% 인 정책이 전이만 하면 이탈 90% → 거울 반전 의심 → 동역학 배제 프로브로 확정:
- MetaDrive `local_coordinates` lateral 은 **오른쪽=+** (왼쪽 0.5m 이동 시 lat=−0.5 실측)
- MetaDrive `heading_diff` 는 **좌회전 시 감소** (정렬 0.500 → 좌 10° 0.413 실측)
- 우리 환경은 둘 다 반대(좌+) → obs idx2/idx8 이 거울상 → 정책이 반대로 보정하며 이탈.
조향 부호는 양쪽 동일(+=좌회전, 프로브 확인). 수정 후 재학습 진행.

**파일럿이 추가로 드러낸 구조적 차이 (본판 설계 반영 사항):**
1. MetaDrive 학습이 `num_scenarios=1` — 같은 교통 배치 하나만 반복 학습(사실상 암기 가능),
   custom 은 매 에피소드 랜덤. **본판은 MetaDrive 도 다수 시나리오로 학습**해야 공정.
2. MetaDrive `reset(seed)` 는 난수 시드가 아니라 **시나리오 인덱스** (base_env.py:921).
3. 맵 구조 차이: MetaDrive X 는 다차선·교차로 반경 10, 스폰 직선이 짧아 체크포인트 거리
   분포가 다름 (스폰 직후 ck1 이 ~4m vs 우리 ~49m). 잔여 도메인 갭의 원천.
4. MetaDrive 조향은 명령 대비 지연(내부 동역학)이 있어 같은 행동열에서도 궤적이 다름.

**파일럿의 결론:** 파이프라인은 작동하고(53배 수집, MD→MD 50% 성공으로 하네스 무결 입증),
교차 전이는 관측 부호 정합이 선행조건임을 실측으로 확인. 부호 수정 후 재학습으로 재검.

### 3.12 정합 라운드 4~5: 규칙 정합 (2026-08-20 오후)

라운드 3까지 관측·기하·동역학을 맞췄는데도 전이 0%(이탈 100%)가 유지되어,
정책 궤적을 MetaDrive 안에서 스텝 단위로 추적했다. 발견:

**라운드 4 — 두 가지 규칙 불일치 (실측 확정)**
1. `on_continuous_line_done=True` 가 MetaDrive 기본값 (`metadrive_env.py:88`):
   백색 실선(로드웨이 가장자리)·황색선(중앙) 접촉 = 즉시 종료.
   우리 환경은 21m 도로 전체가 자유 → 정책이 차선 규율을 배운 적 없음.
2. 스폰 차선이 시나리오마다 랜덤 (8회 실측: 우측 4/가운데 1/좌측 3).
   우리는 항상 가운데 → 비가운데 스폰 관측이 정책에게 분포外.
조치: 36경로(4팔×3기동×3차선별 커넥터), 실선 접촉 종료, 스폰 차선 랜덤화.
결과: in-domain 84%(더 어려운 규칙에서), 전이 리턴 −2 → **+16~21** (처음으로 실주행),
그러나 성공은 여전히 0%.

**라운드 5 — 판정 기하 불일치**
사망 지점 추적: 실선까지 1.26~1.35m 남기고 사망 (중심점 기준으론 생존 거리).
MetaDrive 는 차 중심이 아니라 **차체 폴리곤**으로 선 접촉을 판정 — 차가 기울면
앞모서리가 먼저 닿는다 (반폭 0.93 + 전장절반×sin(각) + 선폭 ≈ 실측 사망거리와 일치).
조치: 유효 마진 = 0.926 + 2.3·|sin(축이탈각)| + 0.15 로 교체. 재학습 진행 중.

**방법론적 소득**: "동일 과제" 정합은 관측·동역학·기하만으로 부족하며
**종료 규칙과 그 판정 기하까지** 일치해야 한다. 각 라운드는 스텝 추적·상태 프로브로
원인을 실측 확정한 뒤에만 수정했다 — 이 절차 자체가 시뮬 정합의 재현 가능한 방법론이다.

### 3.13 라운드 5 결과 — 전이 돌파 (2026-08-20)

차체 모서리 기준 실선 판정(라운드 5) 적용 후:

| 평가 | 성공률 | 비고 |
|---|---|---|
| in-domain (custom) | 83% | 규칙 강화로 98%→83% (더 어려운 과제) |
| **custom→MetaDrive 제로샷** | **30%** | 0%에서 돌파. 충돌 10%/이탈 60% |
| (참고) MetaDrive→MetaDrive | 50% | 동일 30분 예산의 네이티브 학습 |

**전이 성공률이 학습 wall-clock에 단조 상승**: 5분 13% → 30분 30%.
"경량 시뮬에서 더 오래 배울수록 고충실도에서 더 잘한다"가 파일럿 데이터로 확인됨 —
본 실험의 핵심 곡선이 성립한다는 증거.

파일럿 결론: ① 파이프라인 완동 ② 정합 5라운드로 제로샷 전이 달성
③ 남은 격차(이탈 60%)의 다음 용의자는 교차로 내부 차선 커버리지.

## 4. 위협 요인 (미리 적어둔다)

- **자명한 결과 위험**: 동일 wall-clock에서 샘플 수가 ~1000배 차이나므로,
  교차 평가 없이는 "샘플 많은 쪽이 이김"이라는 자명한 결론에 그친다.
- **비교군 표현**: MetaDrive는 스스로를 "lightweight"라 부른다.
  논문에서 "무거운 시뮬"이 아니라 "고충실도 비교군"으로 표기해야 정확하다.
- **하드웨어 제약**: RTX 4060 Laptop 8GB / RAM 14GB. CARLA는 실무 최소 RAM 미달.
  단일 장비 결과라는 외적 타당성 한계를 명시해야 한다.
- **문헌 FPS 값은 남의 장비 기준.** 본 장비 실측치로 대체할 것.

## 5. 진행 로그

- 2026-08-18 — 선행연구 1차 출처 확인, 하드웨어 실측, 주장(동일 wall-clock) 확정, 본 골격 작성
