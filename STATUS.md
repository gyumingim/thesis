# STATUS

## 자율 운행 재개 지침 (세션이 끊겼을 때 여기부터 읽는다)

**게이트 — 절대 넘지 말 것**
- **UE 대량 생성은 사용자 육안 합격 전 금지.** 현재 상태: 검증 10장면 반복 개선 중이며
  **아직 합격받지 않았다.** 합격 없이 `ue/mass_gen.sh` 를 돌리지 않는다.
- **정숙 조건 필수.** 벽시계 예산 실험(bench/run_*.sh)은 UE·CARLA 와 **동시에 돌리지
  않는다.** GPU/CPU 경합이 결과를 조용히 오염시킨다(실측: 네이티브 62.0% → 53.3%).
- **CARLA 배치는 순차 실행.** 두 클라이언트가 같은 서버를 tick 하면 결과가 깨진다.
- **UE 렌더는 단일 인스턴스.** `ue/verify10.sh` 의 lockdir 을 우회하지 않는다.

**재개 절차**
1. `git pull` → **`.venv/Scripts/python.exe`** 로 `tools/paper_numbers_check.py` 와
   `tools/carla_numbers_check.py` 를 돌려 **둘 다 «전 항목 일치»** 인지 확인한다.
   ★ 시스템 파이썬으로 돌리지 마라 — 의존 모듈이 없는 블록이 조용히 빠진다. 실제로
   DPC τ 재산출 검사가 그렇게 죽어 있었는데 출력은 «전 항목 일치» 였다(2026-09-15).
   지금은 건너뛴 블록을 이름과 이유까지 출력하므로, **«검사 건너뜀» 이 한 줄이라도 있으면
   그 실행의 «전 항목 일치» 는 믿지 마라.**
2. UE 작업이 걸려 있었다면 `cat /c/ue/verify10/log` 로 `VERIFY10_DONE` 여부를 본다.
   미완이면 UE 프로세스 수를 확인하고(0 이어야 재실행 가능) lockdir 을 지운 뒤 재실행한다.
3. 검증 렌더가 끝났으면 **두 감사를 돌린다**:
   - `python ue/label_audit.py C:/ue/verify10 <직전_디렉터리>` — 라벨 기하
   - `python ue/render_audit.py C:/ue/verify10 <직전_디렉터리>` — 픽셀 외형
   직전 디렉터리를 함께 주는 것이 중요하다. **이전 자료의 값까지 달라지면 감사 코드를
   의심하라** — 2026-09-02 에 감사 스크립트의 좌표 오류로 없는 결함을 «고친» 적이 있다.
4. 결함이 남아 있으면 고치고 → 이전 렌더를 `verify10_<날짜>` 로 보관 → 재렌더 → 3 으로.
   결함이 없으면 **컨택트 시트를 만들어 사용자에게 육안 판정을 요청하고 거기서 멈춘다.**

**판정 기준 재확인**: "예쁜 사진"이 아니라 **"평범한 실사와 구별 불가"**. 극적인 황혼·역광은
사진으로는 좋아 보여도 목표에서 멀어진다.

---

## 2026-09-17 — 시드 보강 5 → 10 진행 중 (최신)

`bench/run_seeds6to10.sh` 가 2026-09-16 23:39 에 시작돼 **워치독 재시작을 넘어 계속 돌고
있다**. 두 팔을 번갈아 도는 멱등 스크립트이므로, 끊기면 그냥 다시 부르면 된다.

**돌리기 전에 확인한 것** (하나라도 어긋났으면 「혼용 금지」에 걸려 돌리면 안 됐다):
- 학습 경로 코드(ppo/env_numba/md_env/spec)가 확정 런을 만든 **ecfe8c6 과 바이트 동일**.
  확정 런은 08-28 22:20 시작, ecfe8c6 은 같은 날 21:36 커밋. 이후 `bench/` 커밋은 전부
  평가·분석 파일뿐이다.
- **같은 장비** — RTX 5080 데스크톱. 장비 교차 교락(§7)을 만들지 않는다.
- UE·CARLA 0. 정숙 조건 성립.

**진행**(09-17 07:35): **쌍 3까지 완료** — 경량 s6~s9, 네이티브 s7~s9. **런 7/10.**
네이티브 s10 학습 중, 남은 것은 네이티브 s10 · 경량 s10 · 네이티브 s11 (약 3시간).
완료된 일곱 모두 무결(ckpt 12, 에피소드 30 완전, 블록 8/8).
★ 무결성 점검은 **구조만** 본다. 성공률은 일부러 읽지 않는다.

### 사전 등록 — 10시드가 모이면 무엇을 어떻게 읽을지 (숫자를 보기 전에 못박는다)

자료를 본 뒤에 분석을 고르면 어떤 결론이든 만들 수 있다. 이 저장소는 그 실수를 이미 두 번
잡았다(오라클 체크포인트 선택, 마스킹 층화 정의 세 개). **그래서 지금, 아직 아무 성공률도
읽지 않은 상태에서 절차를 고정한다.**

1. **주 지표**: 블록 8개 평균 성공률을 시드별로 만들고, **시드를 단위로** Welch t 검정.
   보고 값 = 팔 평균±σ, 격차, t, df, p, 95% CI. 정의는 지금 `tools/scenario_axis.py` 의
   「정직한 검정」 절과 **한 글자도 바꾸지 않는다**.
2. **부 지표**: (a) 블록별 격차의 부호검정(정확 이항), (b) 시드×블록 분산 성분과
   재계산한 필요 시드 수, (c) b500000 단독 격차(논문 헤드라인과의 연결용).
3. **금지**: 시드 제외, 주 지표 교체, 블록 부분집합 선택, 「유의해질 때까지」 추가 학습.
   경량 s6 가 높게 나온 것을 이미 보았으므로 특히 **조기 중단 금지** — 다섯 쌍 전부 돈다.
4. **결과가 어느 쪽이든 그대로 쓴다.** 격차가 줄어 p 가 올라가도, 5시드 결과를 지우지 않고
   **n=5 행과 n=10 행을 나란히** 남긴다(DPC 표의 전례대로 — 바뀐 것이 표본뿐임이 보이게).
5. **감시 동시 갱신**: `tools/paper_numbers_check.py` 의 시드 목록·기대값을 같은 커밋에서
   고치고, 음성 시험으로 발화를 확인한다. 갱신 전에 **두 검사가 모두 통과하는지 먼저**
   확인한다(정숙 조건 때문에 여러 세션 미뤄 둔 상태다).
6. **순서**: 학습 종료 → 두 검사 통과 확인 → 도구 시드 목록 확장 → 재계산 → 논문 반영.
   계산을 먼저 하고 그 결과를 보고 나서 도구를 고치지 않는다.

★ **경량 s6 의 블록 평균이 약 60%** 로 기존 5시드(48.4%)보다 한참 높다. **지금 읽지 마라.**
  n=1 의 극단값이고, 여기서 멈추거나 이 값을 근거로 무엇을 쓰면 그것이 정확히 **선택적
  중단 편향**이다(§6.2 의 오라클 편향과 같은 종류의 실수). 스크립트는 다섯 쌍을 다 돌고,
  격차 추정은 **10시드가 균형 있게 모인 뒤에만** 갱신한다. 분석 도구의 시드 목록(LIGHT/
  NATIVE)을 일부러 건드리지 않은 이유도 이것이다 — 논문 수치가 도중에 흔들리면 안 된다.

★ **학습이 도는 동안 지켜야 할 것**: 벽시계 예산 실험이므로 CPU/GPU 를 쓰는 작업을 함께
  돌리지 않는다. 몬테카를로가 든 `paper_numbers_check.py` 도 **미룬다** — 재개 점검의
  「두 검사 실행」보다 정숙 조건이 우선이다(직전 실행에서 같은 커밋으로 둘 다 통과했고,
  작업 트리도 깨끗하므로 재실행이 주는 정보가 없다). 학습이 끝난 세션에서 돌린다.

---


## 2026-09-16 — 시드 요구량 재계산 + DPC 널 방어 + 급내 상관 + 블록 8개

**「팔당 25시드(총 50시간) 필요」가 과대였다 — 다시 세니 10시드다.**
이 숫자는 여러 세션째 「승인이 필요한 20 GPU-시간짜리 결정」으로 보고해 온 항목의 근거였다.
`tools/variance_components.py` 로 두 가지를 바로잡았다.

- (i) **표적 격차**: 25시드는 b500000 **한 블록**의 12.7%p 를 표적으로 한 값인데, 그 블록은
  여덟 중 경량에 가장 유리했다(§7 표 6). 블록 평균 19.7%p 를 표적으로 하면 **12시드**.
- (ii) **σ 성분 분해**: 시드마다 블록 8개가 있으므로 시드 × 블록 이원 분해가 가능하다.
  | 팔 | σ_정책 | σ_블록 | σ_잔차 | 이항 바닥 |
  |---|---|---|---|---|
  | 경량 | 9.5 | 0.0 | 9.3 | 8.8 |
  | 네이티브 | 17.6 | 2.2 | 5.9 | 7.9 |
  잔차 ≈ 이항 바닥이므로 **시드×블록 상호작용의 증거가 없다**(정책마다 어려운 블록이
  다르지 않다 — τ-b=+0.414 와 같은 방향). 단일 블록 σ 중 참 정책 분산의 몫은 경량 73%,
  네이티브 96%. 이걸 넣고 블록 8개를 평균하면 **10시드**(평가 잡음 0 으로도 10 = 하한).
- 정리: **25 → 12 → 10.** 큰 몫은 (i) 표적 정정이지 (ii) 분산 분해가 아니다.
  ★ **실무적 함의: 이미 있는 5시드에 팔당 5시드(약 10 GPU-시간)만 더하면 80% 검정력.**
  「50시간」이 「10시간」이 됐으므로, 승인 요청의 성격이 달라진다.

★ 방법 검산을 반드시 먼저 보라 — 같은 도구를 논문의 원래 표적(블록 1개·12.7%p)으로 돌리면
  **26시드**가 나와 기존 25시드를 재현한다. 바뀐 것은 방법이 아니라 표적과 σ 정의다.
★ 경고: 관측 효과크기를 참값으로 놓은 **계획용 눈금**이다. 19.7%p 의 95% 구간이
  [−41.8, +2.3] 이므로 필요한 시드 수도 그만큼 불확실하다. 논문이 경계한 「관측 p 에서
  미래 검정력 역추론」 함정이 여기에도 적용된다.

★ 도구 작성 중 편집 스크립트가 함수 독스트링의 문구를 매칭해 `need_n` 과 `main` 을 통째로
  날렸다. 줄 번호 기반 치환은 **본문에 같은 문구가 있는지부터 확인**해야 한다.

감시 9건 추가(표 두 행 + 시드 수 세 값 + 논문 문장 세 개 + 방법 검산). 음성 시험 확인.

---



**§6.3 의 «경량 신호는 전이 순위를 예측하지 못한다»(τ=+0.097, p=0.513)에 대한 가장
자연스러운 반론 — 「그냥 잡음이라 못 잰 것 아니냐」 — 을 수치로 닫았다.**
상관의 상한은 양쪽 측정의 신뢰도에 걸린다. 재학습 없이 **반분 신뢰도**로 둘 다 쟀다
(`tools/dpc_reliability.py`, 원자료는 어제 만든 `bench_results/clean_perep`).

- **타깃**(12 체크포인트 × 30에피소드 순위): 15/15 분할 두 순위의 스피어만 0.355,
  스피어만-브라운 보정 **0.482**(시드 범위 0.13~0.76).
- **소스**(L_c = ±150s 창의 경량 학습 수익): 같은 절차로 0.933, 시간 자기상관을 고려한
  **보수적 시간 분할로도 0.880**. 즉 「L 의 동적 범위가 평균의 1%」라는 §6.3 의 한계는
  **순위 신뢰도로 환산하면 문제가 아니다** — 작은 차이라도 재현되게 잰다.
- 합친 감쇠 상한 √(r_소스·r_타깃) = **0.65**. 완벽한 예측이어도 관측 τ 는 이 부근이어야
  하는데 실제는 +0.097, 상한의 **15%**.
- → **널 결과를 측정 잡음으로 돌릴 수 없다. 실질적 널이다.** §6.3 결론이 강화된다.

★ 이 도구는 결론을 한 번 뒤집었다. 처음 출력은 「«예측력 없다» 와 «잴 수 없었다» 를
  가를 수 없다」였는데, 신뢰도가 0.482 로 **낮지 않게** 나왔으므로 그 문장은 자료와
  반대였다. 출력문을 먼저 써 두고 숫자를 끼워 넣으면 이렇게 된다.

★ 소스 쪽 무작위 분할은 학습 로그의 **시간 자기상관** 때문에 신뢰도를 부풀린다(0.933).
  시간 분할(0.880)을 함께 내고 **낮은 쪽을 상한 계산에 쓴다.**

★ 남는 것: 타깃 신뢰도의 시드 편차(0.13~0.76)가 크고, 0.8 로 올리려면 약 130에피소드가
  필요하다. 급내 상관 ρ_w=0.612 때문에 **같은 장면을 더 보는 게 아니라 다른 장면**을
  늘려야 한다.

감시 6건 추가. 「계산 → 기록 파일 → 논문」 사슬 전체를 건다 — 타깃 값은 감시가 직접
재계산(1초)하고, 텐서보드 로딩이 20초 넘는 소스 값은 기록 파일과 대조하되 **기록의
최신성은 재계산한 타깃 값으로 확인**한다. 낡은 기록은 그 자리에서 걸린다(음성 시험 확인).

---



**§7 (5) 오라클 편향 판정의 «가정» 을 «측정» 으로 바꿨다.**
그 판정(체크포인트 선택의 겉보기 이득 +15.4%p 는 최댓값 편향)은 장면 난이도의 급내 상관
ρ 를 **가정한 격자**(0/0.3/0.7)로 감도만 봤다. `oracle_bias.py` 자신이 「ρ 를 추정하려면
에피소드 단위 결과가 필요한데 현재 평가는 집계값만 남긴다」고 적어 두고 있었다.

재학습 없이 해소 — 보관 체크포인트 12개 × 5시드를 같은 30장면에서 **에피소드 기록과 함께**
재평가(`bench/run_ckpt_perep.sh`, 약 12분). **60개 조합 전부 총계가 정확히 재현**됐다.

- 사분상관(잠재 프로빗 척도, `tools/scenario_icc.py`)을 재니 구조가 **하나가 아니라 둘**:
  같은 시드의 체크포인트끼리 **ρ_w=0.612**(시드 범위 0.55~0.71), 다른 시드끼리는
  **ρ_b=0.139**. 즉 **장면 난이도의 대부분은 내재적이 아니라 정책마다 다르다.**
  (같은 방향: 팔 수준 난이도 상관 τ-b=+0.414.)
- 옛 널은 한 모수라 둘을 같다고 놓았다. 두 성분 구조를 그대로 넣으니 널 오라클
  **60.5%[52.7, 68.0]**, 편향 몫 **73%**, 관측 64.7% 는 여전히 구간 **안**이다.
  점추정으로 4.2%p 가 남지만 구간 폭이 15%p 라 0 과 구별되지 않는다.
- **판정은 유지되고 근거가 「어느 ρ 에서도」→「잰 ρ 에서」로 바뀌었다.**
- 이진 ANOVA ICC 는 0.442 로 사분상관보다 낮다. 널이 잠재 정규 모형이므로 넣어야 하는
  것은 **잠재 쪽**이다 — 둘을 섞으면 편향 몫을 과대평가한다.

★ 감시를 붙일 때 `subprocess` 미import 로 블록이 죽었는데, **_skip 이 그 사실을 찍어서**
  바로 잡혔다(옛날 같으면 «전 항목 일치» 로 넘어갔다). 재개 지침의 「건너뜀 한 줄이라도
  있으면 믿지 마라」가 실제로 작동한 사례다. oracle_bias.py 가 실측값을 상수로 들고 있어
  소스에서 읽어 대조하는 감시도 넣었다 — 이런 상수는 조용히 낡는 전형적인 자리다.

---



**§7 표 6 이 남긴 두 한계(「블록이 넷뿐」·「난이도 차이의 원인 미규명」)를 둘 다 해소했다.**
재학습 없이 보관 `final.pt` 를 블록 540000~570000 에서 추가 평가(정책 10종 × 4블록 = 40회,
약 10분). 이제 8블록 × 30장면 × 10정책 = 2,400 에피소드.

- **8/8 블록 격차 음수 → 부호검정이 유의해졌다: 정확 양측 p=0.0078** (4블록에서는 0.125).
- 블록 평균 격차 −19.7%p(시드 단위 −19.8%p, Welch p=0.071, 95% CI [−41.8, +2.3]).
  4블록 때의 −18.0%p·p=0.094 를 대체한다.
- **논문이 쓴 블록(−12.7%p)이 여덟 중에서도 가장 유리**하다는 순위는 그대로고, 최악 블록이
  −22.0 → **−29.3%p** 로 벌어졌다.
- 시나리오 축 σ / 시드 축 σ = 0.34~0.70 (4블록 때 0.36~0.80). 여전히 시드 축이 지배적이다.

**난이도의 출처**(`tools/scenario_difficulty.py`, 추가 평가 0):
- **양 팔 전멸 장면 0개.** 경량만 전멸 18장면(8%), 네이티브만 9장면(4%). 즉 격차는
  「경량이 못 푸는 특수 장면」이 아니라 난이도 분포 전체의 이동이다(최빈 2/5 대 4/5).
- 팔 간 장면 난이도 상관 **Kendall τ-b=+0.414**(n=240) — 두 팔이 같은 장면을 어려워한다.
- ★ **실패 양상이 §5.2 와 다르다**: 경량 초과분이 충돌 +9.9%p·이탈 +10.7%p 로 갈린다.
  지지집합 결손의 지문은 **이탈 편중**이었다(마스킹이 이탈만 43.3%p 줄였다). 따라서 V=3
  정정판의 잔여 격차는 지지집합 위반의 잔재가 아니며 기제는 여전히 미규명이다.

★ 도구 버그 둘을 자체 대조가 잡았다: (1) `scenario_axis.py` 의 부호검정이 2^-(n-1) 이라
  「전부 같은 부호」일 때만 맞았다 — 8블록에서 7/8 이 나왔다면 p=0.008 을 찍었을 것이다
  (실제 0.070). 정확 이항으로 교체. (2) `scenario_difficulty.py` 의 자체 τ-b 분모가 틀렸다
  — scipy 와 대조하는 assert 가 즉시 잡았다. **자체 구현을 남기고 대조하는 이유가 이것.**
  (3) 감시가 블록 목록을 하드코딩하고 있어 넷에서 여덟으로 늘려도 옛 표본을 볼 뻔했다 —
  파일 목록에서 유도하도록 바꿨다.

---



**§5.2 검증 (b) 는 «전이가 5%→55% 로 회복된다» 까지만 말하고 기제는 가설로 남겨 뒀다.**
재학습 없이 같은 final.pt 를 같은 30 시나리오에서 다시 평가해(구판 JSON 에는 per_episode
가 없었다) 시나리오 단위로 짝지었다. 총계는 구판을 **정확히 재현**했다.

- **구제는 한 종류의 실패에만 듣는다**: 회복 +50.0%p 중 **이탈 감소가 43.3%p(87%)**,
  충돌 감소는 6.7%p. 전이표(60쌍)에서도 이탈 35건 중 24건(69%)이 성공이 된 반면
  충돌은 22건 중 8건(36%)이고, 마스킹 뒤에도 충돌이 18건 남는다.
- 짝지은 정확검정(McNemar) 성공→실패 2 대 실패→성공 32, **p<10⁻⁵**. 정책 시드는 둘뿐
  이므로 정책 축의 일반화는 여전히 n=2.
- 즉 «지지집합 위반의 주된 피해» 의 실체는 **분포외 z-score 로 인한 정책 폭주 = 도로 이탈**
  이다. (i) 확인.

**(ii)(「없는 셈 친 셋째 차」 때문에 충돌이 남는다)는 이 자료로 검증 «불가»다.** 층화
정의 셋을 모두 시도했고 셋 다 무너졌다 — 이 실패를 그대로 남긴다:
1. 종료 시점의 이웃 거리 → **순환**. 충돌한 순간엔 들이받은 차가 정의상 가깝다.
   마스킹 V=2 와 재학습 V=3 이 똑같이 +63/+65%p.
2. 종료 직전을 뺀 전 구간 노출비 → **에피소드 길이와 교란**. 일찍 죽으면 분모가 짧아
   노출이 낮게 나와 부호가 뒤집힌다(−3/−18%p).
3. 길이와 무관한 앞 20스텝 고정 구간 → **노출 높음 층이 빈다**(0건). 셋째 차는 교차로
   에서야 모이고, 교차로에 가려면 이미 실패하지 않았어야 한다.

★ **규칙으로 남긴다**: 결과가 정해진 뒤의 상태로 층화하면 부호는 정의를 따라 움직인다.
  통제군(여기서는 V=3 재학습 정책) 없이 층화 결과를 읽지 마라. 그리고 **정의를 더 찾는
  것은 유의한 것이 나올 때까지 고르는 일**이므로, 무너진 지점에서 멈추고 그 이유를 적는다.
  PAPER §5.2 에 반영, §8 (10) 에 «위협 차량을 통제한 설계» 로 남겼다.
  도구: `tools/mask_mechanism.py`, `bench/mask_threat_probe.py`, `bench/run_mask_paired.sh`.
  원자료: `bench_results/mask_paired/`. 감시 9건을 paper_numbers_check 에 추가.

---


## 2026-09-02·09-05 — 심사 시뮬레이션 반영과 UE 외형 감사

**논문 (커밋 44c39e6 ~ 9767a40)**
- **교차 구조 검정 신설** — 개별 시점의 격차는 다중비교를 통과하지 못하지만, 주장의 내용인
  «두 곡선의 기울기가 다르다» 는 검정된다: 시드별 Δ(최종−5분)가 경량 −5.3%p 대 네이티브
  +25.3%p, 차이 **+30.7%p, 정확 순열 양측 p=0.0079**(10분 앵커 +21.3%p, p=0.0238).
  `tools/crossing_test.py`
- **표 5 설명 철회 (A1)** — 「필요 횡가속 0.79 g 가 거버너 한계에 붙어서 우회전이 어렵다」와
  「실패율이 최소 반경을 따라간다」를 철회했다. 좌회전이 0.83 g 로 **더 높은데 성공률도
  높고**(77.8% vs 54.2%), 반경 14.9 m 좌회전이 33.1 m 직진보다 덜 실패한다. 「좌회전은
  균일」도 정책당 3건이라는 분모의 착시였다(분산/이항잡음 우회전 2.14배 vs 좌회전 1.13배).
  남는 것은 정책 대응 좌−우 차이 +23.6%p, 역전 0/9, 부호 순열 p=0.031. `tools/table5_filters.py`
- **반복 라운드 군집 보정 (A2)** — 표 2·3·4 의 반복도 매 라운드가 **같은 20 앵커**를 돈다
  (turn_deg·min_R 수열 라운드 간 동일, 14/14 조건). deff 2.6~6.3, **유효 표본 19~23**.
  조건 간 비교를 대응 검정으로 대체 — 표 3 마스킹 효과 +41.7%p(p=0.0039)는 유지되고,
  거버너 효과 소멸이 검정된다(구성A +30.8%p → 구성B +0.0%p, 상호작용 p=0.039).
  「마스킹을 켜면 거버너가 +13.3pp」는 p=0.25 라 철회. `tools/carla_cluster_ci.py`
- **평가 시나리오 축 한계 명시 (A3)** — 모든 MetaDrive 전이 수치가 시나리오
  **500000~500029 라는 단일 30장면 표본**에 조건부다. 오차막대에 시나리오 축이 없다.
  `bench/evaluate.py` 가 `per_episode`·시나리오 인덱스를 남기도록 수정.
- **검정력 명시 (B2)** — 「표본을 늘려도 쉽게 유의해지지 않는다」류 사후 검정력 역추론 2곳
  삭제. 사전 검정력은 d=0.83 에서 **0.19**, MDE 31.9%p, 80% 검정력에 팔당 25시드 필요,
  TOST 경계 ±30.7%p 라 **동등성도 주장 불가**. `tools/power_mde.py`

**UE 생성기 — 09-06 3차 (커밋 e689795 ~ 9efe7b2) · 육안 게이트 아직 미요청**

층화 검증 세트(12장 = 프리셋 6종 × 2장)에서 **라벨 감사 전항목 청정**, 외형 판정은
2 통과 · 1 실패 · 1 철회:

| 판정 | 구판(0902) | 현재 | |
|---|---|---|---|
| (2) 백화 | 12.3% | **4.9%** | 통과 |
| (4) 소실점 공허 | 최대 17.8% / 평균 6.8% | **최대 7.0% / 평균 3.8%** | 통과 |
| (1) 하늘 색 분리 | 맑음 0.020 vs 흐림 0.150 | 맑음 0.010 vs 흐림 0.014 | **실패 — 역전은 사라졌으나 분리가 없다** |
| (3) 노면 대비 | — | — | **판정 철회(교락)** |

- **난수 층 분리가 반쪽이었다** — 건물 배치가 프리셋과 조명 사이에서 난수를 써서, 타워를
  2행 → 4행으로 늘리자 **손대지 않은 맑음 장면이 주황색이 되고 판정이 뒤집혔다**. 조명 층
  진입 직전에도 재파종해 네 층(프리셋 → 기하 → 조명 → 배치)을 독립시켰다.
  **교훈: 「A 를 고쳤더니 무관한 B 가 나빠졌다」면 난수 스트림 공유부터 의심하라.**
- **감사 판정이 표본 2개에 얹혀 있었다** — 가중 추첨이면 맑음 계열이 10장 중 2.2장뿐이라
  기하만 바꿔도 통과↔실패가 뒤집힌다. `CS_PRESET_CYCLE` 로 검증에서만 프리셋을 순환시키고
  기본 장면 수를 12로 올렸다(맑음 계열 2 → 4장). 생성기 기본은 실제 빈도 유지.
- **하늘 마스크를 세 번 고쳤다** — 「상단 100행」→「밝기 상위」→「매끄럽고 밝은」→
  **「건물 윤곽선 위」**. 앞의 셋은 전부 벽면·유리를 쟀다. 고칠 때마다 구판을 같은 지표로
  다시 돌려 대조했다(구판은 새 마스크에서 **더 뚜렷하게** 실패한다: 0.020 vs 0.150).
- **노면 대비 판정은 철회** — 흐림 계열 3종이 전부 wet, 맑음 계열이 전부 dry 라 습윤 반사와
  그림자 경도가 완전 교락이다. 없는 근거로 통과를 주장하지 않기 위해 통과/실패를 매기지 않는다.

**노면 재질 추적 결과 (09-06 새벽, 커밋 f474618) — 위 「다음 목표」의 답**

「맑음 장면 노면이 젖어 보인다」를 세 단계로 좁혔고, **중간에 한 번 틀린 결론에 도달할
뻔한 것을 통제 실험이 막았다.**

1. 스왑은 정상 작동한다 — 계측 로그: 액터 74개 · 슬롯 1개 ·
   `M_Asphalt_Master_Inst → M_Freeway_Asphalt`. 「슬롯이 여러 개」 가설 기각.
2. **「wet/dry 가 뒤집혔다」고 결론할 뻔했다.** 맑음(gen_4)과 흐림(gen_0)을 비교하니
   마른 프리셋이 어둡고 푸르고(0.290, B−R +0.133) 젖은 프리셋이 밝고 중성(0.508, −0.016)
   — 완벽한 역전으로 보였다. 그러나 두 장면은 태양도 건물도 다르다. **같은 장면·같은
   프리셋에서 재질만 바꾸는 통제 A/B** 를 돌리니 freeway 0.290 vs default 0.281 —
   **차이 3%**. 재질은 거의 무관했다. 「젖어 보임」의 원인은 저각 시점의 스카이 반사이며
   그것은 물리적으로 정상이다.
3. 도로 재질 4종 전수 비교(같은 장면): 기본 0.281 · freeway 0.290 · MI_Road 0.320 ·
   MI_RoadDark 0.263. **젖은 노면을 만드는 재질은 이 프로젝트에 없다** — 어느 것도 젖음의
   지문(어두움 + 강한 정반사 + 높은 하늘 반사)을 내지 않는다.

→ `wet` 플래그를 「습윤」으로 부르는 것은 과장이므로 실제로 얻을 수 있는 것만 취해
**노면 밝기 축**으로 쓴다: 젖은 계열 → RoadDark(0.263), 마른 계열 → Road(0.320).
구판의 3% 무동작을 **22% 차이**로 대체했다. `CS_FORCE_ROAD_MAT` 로 재질을 강제하는
A/B 스위치를 남겼다(`/c/ue/mat_ab.sh`).

**교훈 (난수 교락과 같은 계열)**: 조건이 둘 이상 다른 두 장면을 비교해 원인을 귀속하지
마라. 여기서도, 그리고 09-02 의 「역주행 8.5%」에서도, **통제하지 않은 비교가 없는 결함을
만들어 냈다.** 생성기 변경 전에 「같은 장면에서 그 인자만 바꾼 A/B」를 먼저 돌린다.

**재질 축 투입 후 검증 결과 (12장 층화, 커밋 ab0ce4c 이후)**

| 판정 | 0906b(재질축 전) | 현재 |
|---|---|---|
| (1) 하늘 색 분리 | −0.004 실패 | −0.020 실패 |
| (2) 백화 | 4.85% 통과 | 5.11% 실패(경계) |
| (4) 소실점 공허 | 최대 7.0% 통과 | 최대 10.8% 실패 |
| 라벨 감사 | 전항목 청정 | **수치까지 완전 동일** |

두 가지가 확인됐다. (a) **하늘 색 분리는 노면과 무관한 별개 문제다** — 예고한 대로
재질 축을 넣어도 개선되지 않았다. (b) 라벨 감사 수치가 **완전히 동일**하다(239 라벨,
모든 항목 일치) — 조명만 바꾸고 기하는 그대로라는 뜻이므로 **난수 층 분리가 설계대로
작동함이 실증됐다.**

다만 (2)·(4) 가 경계에서 악화됐다. 어두운 노면 → 자동노출 상승 → 백화·«밝고 매끄러운
화소» 증가라는 일관된 기제다. **판정값들이 실행 간 변동 폭이 쫓고 있는 효과와 비슷하다** —
0906b 대비 (4) 가 7.0 → 10.8 로 움직였는데 기하는 한 글자도 안 바뀌었다.

**판정 변동 측정 결과 (09-06 새벽, 커밋 31af8f8) — 위 목표의 답, 그리고 자기 정정**

같은 코드로 시드만 바꿔 3회 렌더(각 12장 층화)했다. `ue/audit_noise.py`

| 지표 | 평균 | 표준편차 |
|---|---|---|
| 소실점 공허 최대 | 12.63% | **2.09%p** |
| 최대 과노출 | 4.56% | **2.28%p** |
| 하늘 B−R 맑음 | 0.009 | 0.012 |

**직전 보고를 정정한다.** 나는 (4) 소실점을 「7.0% 통과」, (2) 백화를 「4.85% 통과」로
보고했으나 세 시드 평균은 12.6%·4.56% 다. **7.0 은 운 좋은 한 판이었고 (4)는 그 임계를
통과한 적이 없다.** (2)는 σ가 임계까지의 여유(0.44)보다 다섯 배 커서 단일 실행으로는
애초에 가릴 수 없었다 — 「4.85 통과 → 5.11 실패」 비교는 무의미했다.

**임계 재교정 (커밋 1e0a530) — 내가 고른 두 임계가 실사도 못 지키는 값이었다**

실사 150장(Udacity CrowdAI)에 같은 지표를 돌렸다. `ue/real_baseline.py`

| 지표 | 실사 중앙 | 75분위 | 최대 |
|---|---|---|---|
| 소실점 공허 | 2.6% | 6.1% | 27.8% |
| 과노출 | **7.5%** | 10.9% | 36.6% |
| 하늘 B−R | +0.008 | +0.020 | +0.561 |

내 임계는 「소실점 최대 8%」·「과노출 최대 5%」였다. **과노출 5%는 실사 사진 절반을
떨어뜨리는 기준**이고, 우리 렌더는 오히려 실사보다 **11배 덜 탄다**(0.68% vs 7.5%).
최대값 판정도 실사에 불공정하다. 둘 다 **장면 중앙값 × 실사 75분위**로 바꿨다.

**재교정 후의 정직한 상태 (3시드)**

| 판정 | 값 | |
|---|---|---|
| (2) 백화 | 중앙 0.68% ± 0.34 (임계 10.9%) | 통과 — 여유가 σ의 30배 |
| (4) 소실점 | 중앙 4.67% ± 1.23 (임계 6.1%) | 통과 — 여유 1.43이 σ와 비슷해 경계 |
| (1) 하늘 색 | −0.028 ± 0.024, 3/3 음수 | **방향 일관, 미확립**(부호검정 p=0.25, t=−2.05) |
| (3) 노면 대비 | — | 판정 철회(습윤 교락) |
| 라벨 감사 | 전항목 청정 | |

**교훈 셋 (모두 같은 계열 — 통제되지 않은 비교)**
1. 조건이 둘 이상 다른 두 장면으로 원인을 귀속하지 마라(노면 재질 「역전」).
2. 단일 실행의 통과/실패를 믿지 마라. σ를 먼저 재고, 그보다 작은 차이는 쫓지 마라.
3. **임계를 스스로 만들지 마라.** 「실사와 구별 불가」가 목표면 임계는 실사 분포에서 온다.

**노출 되돌림 결과 — 귀무 (커밋 68283ac, `/c/ue/noise2_*`)**

−0.55 → −0.20 EV 를 3시드로 검증했다. **어느 판정도 잡음을 넘어 움직이지 않았다**:

| 지표 | −0.55 EV | −0.20 EV | σ | 판정 |
|---|---|---|---|---|
| 과노출 중앙 | 0.68 | 0.76 | 0.33 | 변화 없음 |
| 소실점 공허 중앙 | 4.67 | 5.05 | 1.2~1.4 | 변화 없음 |
| 하늘 색 분리 | −0.028 | −0.020 | 0.024~0.036 | 변화 없음 |

시드 하나만 봤을 때 「하늘 색 분리가 −0.019 → +0.010 으로 뒤집혔다」로 보였으나
나머지 두 시드는 반대로 갔다 — **내가 세운 규칙이 내 관찰을 걸러냈다.**

그리고 노출로는 백화를 실사에 맞출 수 없음이 확인됐다. 0.35 EV 를 올려도 과노출은
0.47 → 0.58% 로 멈춘다(실사 7.5%). **차이의 원인은 노출이 아니라 장면 구성**이다 —
참조는 하늘이 넓게 열린 간선도로이고 우리는 하늘이 좁은 도심 협곡이다. 노출을 더 올려
억지로 맞추면 다른 것이 망가진다. 값은 덜 인위적인 −0.20 으로 두되, **이 변경이 무엇도
개선하지 않았음을 명시**한다.

## 2026-09-11 — 평가 시나리오 축 측정 (논문 한계 하나 해소)

게이트가 열리기 전에 UE 와 무관한 논문 과제를 하나 끝냈다. §7 이 «시나리오 축의 변동은
한 번도 측정되지 않았다» 로 남겨 둔 한계다.

**재학습은 필요 없었다** — 한계가 **평가** 표본에 관한 것이므로 보관된 `final.pt` 를 다른
시나리오 블록에서 다시 평가하면 된다. 체크포인트가 로컬(`runs/`, 198개)에 남아 있었다.
`bench/run_scenario_blocks.sh` (블록 4개 × 정책 10종 × 30에피소드, 약 10분).
원 블록 재현은 시드별로 **정확히 일치**했다(49.3%·62.0%) — 하네스 검증까지 된 셈이다.

| | b500000(논문) | b510000 | b520000 | b530000 | 평균 |
|---|---|---|---|---|---|
| 경량 | 49.3% | 46.7% | 45.3% | 52.0% | 48.3% |
| 네이티브 | 62.0% | 65.3% | 67.3% | 70.7% | 66.3% |
| **격차** | **−12.7%p** | −18.7 | −22.0 | −18.7 | **−18.0** |

- **논문이 쓴 블록이 경량에 가장 유리했다.** 초록·§6.2·§7 에 모두 명시했다.
- 블록 평균 격차 −18.0%p (Welch t=2.01, df=5.7, p=0.094) — 단일 블록 0.227 보다 강하나
  여전히 0.05 미만은 아니다(검정력 한계가 지배).
- **시나리오 축 σ 가 시드 축보다 좁다**(비 경량 0.80 · 네이티브 0.36). 다른 30장면을 썼어도
  결론이 흔들리지 않는다는 것이 수치로 뒷받침되고, 시드 기준 오차막대도 사후 정당화된다.
- 분석에서 함정 둘을 피했다: `clean_sN`·`nd_sN` 은 짝이 아니므로 대응 검정 근거가 약하고,
  5시드 × 4블록 = 20쌍을 독립으로 세면 군집을 무시한다(CARLA 표에서 잡았던 것과 같은 실수).

§8 향후 연구 (7) 을 «완료» 로 바꿨다. 남는 것은 블록 수를 늘린 재확인과 블록 간 난이도
차이의 원인 규명이다.

## ⏸ 육안 게이트 요청 지점 (2026-09-06 05:00)

**자동 계측이 분해능 한계에 도달했다.** 남은 두 판정 모두 σ가 임계까지의 여유와
비슷하거나 크다(소실점 여유 1.05 < σ 1.40; 하늘 색 2/3 실행에서 갈림). 이 상태에서
생성기를 더 만지면 **잡음을 쫓는 것**이다. 판단은 사람 몫으로 넘긴다.

- 제출물: `C:/ue/noise2_90000/` — 층화 12장(프리셋 6종 × 2), 현행 코드
  - `contact_sheet.png` (라벨 상자 포함) · `plain6.png` (프리셋 대표 6장, 상자 없음)
- 계측 상태: 라벨 감사 **전항목 청정** / 백화 통과(여유 σ의 30배) / 소실점·하늘 색 미해결
- 내 눈에 남아 보이는 것(계측이 못 잡는 것): **프리셋 6종이 여전히 비슷해 보인다**,
  **건물이 2~3종만 반복된다**, 노면이 모든 조건에서 젖어 보인다
- **합격 시**: `ue/mass_gen.sh` 로 대량 생성 → 인지 4-arm 재실행(§8 향후 연구 5-c)
- **불합격 시**: 위 세 가지 중 무엇이 결정적인지 알려주면 그것부터 잡는다

### 게이트 대기 중 처리한 것 (09-06 오전, 커밋 6561edd~7625cff)

게이트가 열리기 전에도 확실한 결함 하나는 잡을 수 있었다 — **건물이 2종뿐**이라는 것은
계측 잡음과 무관한 사실이었다.

**건물 풀 2종 → 28종.** 라이브러리의 히어로 빌딩 39종을 기록된 규약대로 1종씩 단독
빌드+렌더로 검증했다(38 통과 · 1 실패: `Tower_CHJ_A01` 은 빌드는 되나 렌더에서 죽는다).
바운드는 **-game 에서 실측**했다 — 커맨드릿에서는 LevelInstance 바운드가 미형성이라
잴 수 없고, 기존 2종의 값도 에디터 세션 수기 기록이었다. 실측 반폭은 19.6~106.8 m 로
크게 갈려, 보수적 상수 하나로 때웠다면 좁은 건물은 멀어지고 넓은 건물은 파고들었을 값이다.
용도로 나눴다: 측면 벽 20종(반폭 ≤45 m 라야 «벽» 이 된다) · 배경/소실점 28종.

**그 확장이 드러낸 내 이전 버그.** 풀을 넓히자 12장 중 3장에서 건물이 도로 위를 지나갔다.
원인은 풀이 아니라 09-05 에 내가 넣은 **배경 스카이라인 열에 회랑 검사가 없던 것**이다.
소실점 타워는 도로 끝 너머에 서므로 침범할 수 없지만 배경 열은 도로 옆구리를 따라가고,
거기에 반폭 106.8 m 짜리가 들어오면 90 m 밴드에서 도로를 덮는다. 측면 경로가 이미 하던
검사를 여기서만 빠뜨렸다. 검사를 넣어 대부분 사라졌고, 남은 1장을 위해 20 m 마진을 뒀다.

**미결이었던 피벗 오프셋 — 해결 (09-06 오전, 커밋 2a71778)**

실패의 진짜 원인은 코드도 로그도 아니라 **스트리밍 지연**이었다. `-game` 에서 22 s 시점에
LevelInstance 액터 조회가 **0개**를 돌려주고, 52 s 에 재면 21개다. `SHOT_DELAY` 를 55 로
올리자 바로 나왔다. (그 전까지 «-abslog 가 안 생긴다», «액터가 0개다» 를 각각 다른 문제로
쫓았는데 하나였다.)

실측: `Bldg_Hero_Mid_NYG_Modern_A01` → **off_x=−3.3 m · off_y=+5.5 m**.
요각이 무작위이므로 유효 횡오프셋은 **반경 6.4 m 안에서 어느 방향으로도** 나타난다.
측면 후퇴거리가 1.5~4.5 m 이니 최악의 경우 형상이 회랑 안으로 **1.9~4.9 m** 들어온다 —
렌더에서 건물이 도로 위를 지나가던 것(12장 중 3장)의 정확한 크기이자 원인이다.
차량은 이미 `mesh.get_bounds().origin` 으로 이 보정을 하는데(생성기 `_bo`) 건물만 안 하고
있었다. 커맨드릿에서 LevelInstance 바운드를 못 재기 때문이다.

조치: `occupancy_half_w_cm` 에 `BLDG_PIVOT_MARGIN_CM=800` 을 더했다. 측면·배경 두 경로가
같은 함수를 쓰므로 한 곳만 고치면 된다. 배경 열에만 임시로 뒀던 +20 m 특례는 제거(밀도 회복).
**한계**: 28종 중 1종만 쟀다 — 나머지가 더 클 수 있어 여유를 얹은 8 m 다. 전 종을 재면
종별 오프셋으로 대체해 마진을 없앨 수 있다.

**프로브 하네스 교훈 (다음 세션이 같은 함정에 빠지지 않도록)**
- `-game` 에서 LevelInstance 를 조회하려면 **SHOT_DELAY ≥ 55**. 짧으면 조용히 0개다.
- 결과는 UE 로그가 아니라 **파이썬에서 파일로 직접 쓴다**. UE 는 기존 로그를 backup 으로
  돌리므로 같은 로그 이름을 재사용하면 직전 실행 내용을 읽는다(실제로 오판했다).
- 「가장 큰 액터」로 건물을 찾으려다 `GameplayDebuggerCategoryReplicator`(바운드 10 km)를
  집었다. **되는 것을 굳이 바꾸지 마라** — 클래스 조회가 맞다.
- 이 저장소의 Bash 호출에서 **여러 UE 실행을 한 명령에 묶으면 exit 255 로 죽는다.**
  한 호출에 UE 하나씩, 또는 백그라운드 스크립트로 돌린다.

### 도로 위 건물 — 해결 (09-06 오전, 커밋 818ffbd·그 다음)

원인 추적이 두 번 빗나갔다가 **JSON 건물 기록**을 넣고 나서야 잡혔다.

1. 처음엔 «피벗 오프셋» 을 원인으로 보고 8 m 마진을 넣었다. 3시드 실측이 그 대가를
   보여줬다 — 소실점 공허 중앙이 5.05 → **7.11%**(σ 0.45)로 임계 6.1 을 넘겨 판정이
   뒤집혔고, **그러고도 넘침은 남았다.** 건물을 밀어내면 협곡이 열린다. 안전과 공허를
   맞바꿨는데 안전도 못 산 셈이다.
2. 바운드 프로브가 «액터 0개» 를 계속 냈다. 하네스를 의심했으나 원인은 **프로브 장면에
   건물이 실제로 0동**이었다 — `CS_BLDG_ONLY` 가 반폭을 35 m 로 가정하는데 8 m 마진이
   겹쳐 모든 배치가 기각됐다. **JSON 건물 기록이 `buildings: 0동` 으로 바로 알려줬다.**
   진단 장치가 없었으면 또 하네스를 뜯었을 것이다.
3. 방향 전환: 마진으로 미지수를 덮는 대신 **검증된 기하 영역으로 되돌렸다.** 측면 풀을
   원래 두 종과 같은 크기대(반폭 ≤33 m, 8종)로 좁히고 큰 건물은 배경 풀(28종 전부)에만
   남겼다. 마진은 3 m 로 낮췄다.

**검증(두 가지 독립 수단)**: 기록된 248동을 기하로 검사해 도로 구간(0~125 m)에서 회랑을
침범하는 건물 **0동**, 그리고 문제 장면(scene_7)이 육안으로도 깨끗하다.

**현재 판정(단일 실행)**: 라벨 감사 전항목 청정 · 백화 통과(0.3%) · **소실점 7.1% 로
여전히 임계 6.1 미달** · 하늘 색 방향 여전히 반대(미확립). 소실점은 건물 작업 전 3시드
평균 5.05 보다 나쁘다 — 측면 풀을 좁혀도 회복되지 않았으므로 배경 열 밀도 쪽을 봐야 한다.

### 소실점 공허 대응과 «머리 위 슬래브» 의 정체 (09-11)

**배경 열 밀도.** JSON 건물 기록으로 분포를 보니 150 m 밴드가 굶고 있었다(장면당 0.3동,
90 m 밴드는 4.5동). 슬롯마다 한 번만 뽑고 기각되면 버리는 구조였다. 슬롯당 6회 재시도 +
90 m 밴드 간격 5.5 → 4.5 m 로 **6.2동/장면**까지 올렸다. 150 m 밴드는 재시도로도 간격을
넓혀도 안 살아난다 — 그 자리가 이미 점유돼 있기 때문이다(반폭 106.8 m 짜리가 150 m 에
서면 AABB 가 y=40 m 까지 뻗어 90 m 밴드·소실점 타워와 통째로 겹친다).

**3시드 판정**: 소실점 공허 중앙 7.11 ± 0.45 → **6.59 ± 1.24**. 「명백한 실패」(여유
1.01 > σ)에서 **「판정 불가」**(여유 0.49 < σ)로 옮겨왔다. 단일 실행에서 본 5.38 은
넓어진 분포의 운 좋은 끝이었다. 하늘 색도 3/3 실패 → **2/3 통과**(평균 −0.003 ± 0.020).
둘 다 n=3 으로는 통과/실패를 말할 수 없다.

**「건물이 머리 위로 넘어온다」는 이제 결함이 아니다.** 게이트 세트에서 다시 그렇게 보이는
장면이 있어 기하로 검사했다 — **262동 중 회랑 침범 0동**, 가장 가까운 건물도 여유 18.4 m
(요구 14 m). 카메라가 90° 화각·높이 1.5 m 라, 50 m 옆에 선 100 m 넘는 건물의 윗부분이
화면에서 도로 위로 수렴하는 **원근 현상**이다. 실제 광각 거리 사진과 같은 모습이므로
**다음 세션은 이것을 결함으로 «고치지» 마라.** 진짜 침범은 기하 검사로 판별한다:
`buildings` 필드에서 |y| − 점유반폭 < 14 m 인 건물이 있는지 본다.

### ⚠ 소실점 공허 추격을 철회한다 — 참조 도메인이 틀렸다 (09-11)

시드를 더 늘려 판정을 분해하려다 비용을 계산해 보니(σ 0.25 까지 = 25시드 ≈ 10시간)
추정량부터 다시 봤고, **비교 자체가 성립하지 않고 있었다**: 실사 기준선은 150장을
풀링해 분위수를 냈는데 렌더 쪽은 «장면 12개의 중앙값» 을 실행마다 구해 평균했다.

36장을 풀링해 **두 참조**와 같은 방식으로 견주니 결론이 갈린다:

| 지표 | 렌더 중앙 | 간선도로 참조(Udacity) | 도심 참조(ref_all) |
|---|---|---|---|
| 소실점 공허 | 7.0% | 2.6% (**+4.3%p 초과**) | 9.2% (**−2.2%p 미달**) |
| 과노출 | 0.8% | 7.5% (−6.7%p) | 3.3% (−2.5%p) |

**소실점 공허는 결함이 아니었다.** 같은 수치가 간선도로 기준으로는 «너무 비었다»,
도심 기준으로는 «오히려 덜 비었다» 가 된다. 우리 장면은 도심 협곡이므로 도심 참조가
맞고, 그 기준으로 우리는 이미 충분하다. **며칠 동안 간선도로 사진에서 뽑은 임계를 쫓고
있었다** — 배경 열 밀도 작업도 그 추격의 일부였다(해롭지는 않았지만 필요하지도 않았다).
`render_audit` 의 판정 (4)를 철회했다(수치는 진단용으로 유지).

**진짜로 남는 것은 과노출이다.** 두 참조 **모두**에 대해 낮다(간선 1/9, 도심 1/4).
하이라이트가 거의 날아가지 않는 것은 도메인과 무관한 분포 차이다. 다만 노출 보정으로는
안 움직인다는 것을 이미 확인했다(−0.55 → −0.20 EV 에서 0.47 → 0.58%). 원인은 노출이
아니라 **장면에 탈 만한 밝은 원천이 없다는 것** — 하늘이 좁고 정반사체가 적다.

**분포 비교 도구**: `ue/dist_compare.py` (REF_DIR 로 참조 전환). 임계 하나가 아니라
분위수·KS 로 견준다. 목표가 «구별 불가» 이므로 비교 대상도 분포여야 한다.

**게이트 제출물 (09-11 최신)**: `C:/ue/noise5_90000/` — 12장 층화, 건물 측면 8종·배경
28종, 회랑 검사 + 배경 열 밀도 보강. `plain6.png` · `contact_sheet.png`

| 항목 | 3시드 결과 |
|---|---|
| 라벨 감사 | **전항목 청정** (역주행 0 · 도색 밟기 0 · ignore 누락 0 · 박스 범위 밖 0) |
| 회랑 침범 | **0/262동**, 최근접 여유 18.4 m (요구 14 m) |
| (2) 백화 | 1.18 ± 0.55 (임계 10.9) — **통과**, 여유가 σ의 18배 |
| (4) 소실점 | **판정 철회** — 참조 도메인이 값을 지배(위 항 참조) |
| (1) 하늘 색 | −0.003 ± 0.020, 2/3 통과 — **판정 불가** |

**명백히 실패하는 판정은 더 이상 없다.** 남은 둘은 n=3 으로 분해되지 않으므로, 더 만지려면
시드를 늘려 «검출 가능하게» 만드는 것이 먼저다. 그 전에 생성기를 더 손대면 잡음을 쫓는 것이다.

**UE 생성기 — 09-05 2차 (커밋 4ab163a ~ f51109e)**
검증 렌더를 세 번 돌리며 계측 → 수정 → 재계측 고리를 돌렸다. `ue/render_audit.py` 판정
세 항목이 **구판 0/3 → 현재 3/3 통과**(같은 지표로 구판을 다시 돌려도 여전히 실패한다 —
고친 것이 지표가 아니라 생성기임을 확인했다):

| 지표 | 0902 구판 | 0905a 1차 | 0905b 2차 |
|---|---|---|---|
| 하늘 B−R 맑음 | 0.035 | 0.035 | **0.096** |
| 하늘 B−R 흐림 | 0.075 | 0.071 | 0.075 |
| 하늘 색 분리 | 실패 | 실패 | **통과** |
| 노면 대비 분리 | 실패 | **통과** | 통과 |
| 최대 과노출 | 12.3% | 9.3% | **4.8%** |

- **차선 파선 길이는 실측으로 정했다** — 같은 장면을 도색 3·6·12 m 로 렌더해 행당 검출
  선 개수를 세니 3.25 / 3.25 / **4.55**. 3·6 m 는 실선만 세어진 값과 같아 파선이 안 보인다.
  12 m / 주기 24 m 채택(`/c/ue/dash_probe.sh`).
- **측방 공허 폐쇄** — 측면 건물 행의 20% 공백 틈으로 빈 지면과 가짜 지평선이 보였다.
  120 m·200 m 배경 열(장면당 ~14동) + 원경 지면을 x −100 m 부터 덮도록 확장.
- **난수 층 분리 (두 번에 걸쳐)** — 처음엔 조명 코드를 고치면 차량 배치가 통째로 바뀌어
  «같은 장면 전후 대조»가 불가능해서 기하 층 진입 직전에 재파종했다. 그런데 **반대 방향이
  열려 있었다**: 건물·타워 배치가 프리셋 선택과 조명 파라미터 사이에서 난수를 쓴다. 소실점
  타워를 2행 → 4행으로 늘렸더니 노출·태양고도·색온도 추첨이 밀려 **손대지 않은 맑음 장면이
  주황색으로 물들고 판정 (1)이 통과 → 실패로 뒤집혔다.** 건물을 늘린 것이 하늘 색을 바꾼
  것처럼 보이는 가짜 인과다 — 지표를 믿고 조명을 또 만졌으면 없는 문제를 쫓을 뻔했다.
  조명 층 진입 직전에도 재파종해 **네 층(프리셋 → 기하 → 조명 → 배치)** 을 독립시켰다.
  **교훈: 생성기에서 «A 를 고쳤더니 무관한 B 가 나빠졌다» 면 난수 스트림 공유부터 의심하라.**
- **역주행 재판정** — 09-02 에 «8.5%» 로 보고했다 철회한 현상이 **실재하되 2.6%**임을
  고친 감사로 확인. 자유 요각 전체가 아니라 «도로축 정렬 ∧ 차로 중심 ±0.6 m» 조합만
  버리도록 좁게 막았다(자유 요각 배치의 4.2%만 기각).

**UE 생성기 — 09-02 1차 (커밋 e885635 ~ 4ab163a)**
- **차선 도색이 차로 «중심»에 그려져 있었다** — `LANE_LINES_CM` 이 `LANE_CTRS×100` 과
  문자 그대로 같은 수라 모든 주행차가 흰 선을 타고 달렸다. 경계값으로 정정 + 모듈 로드 시
  assert 로 회귀 방지. **09-02 렌더에서 수정 확인.**
- **차로 구분선을 파선으로** (도색 3 m / 주기 12 m). 균일한 연속 실선 8줄은 도로가 아니라
  주차장 구획으로 읽혔다. 파선은 타일 건너뛰기로 못 만든다(타일이 20 m) — 스케일 0.6.
- **기상 프리셋이 이름뿐이었고 하늘 색은 실제와 반대였다** — 흐림 계열 하늘 B−R 0.137 >
  맑음 0.043. SkyAtmosphere Mie 산란·태양 원반색·SkyLight 세기로 정정. **검증 대기.**
- 감사 도구 둘: `ue/label_audit.py`(라벨 기하) · `ue/render_audit.py`(픽셀 외형)

**기록해 둘 실패**: 「차로 위 차량의 8.5% 가 역주행」은 **내 감사 스크립트의 좌표 오류**였다.
카메라 상대 y 를 세계 y 로 되돌리며 요각 항을 빠뜨려(110 m 에서 최대 11 m) 없는 결함을
보고하고 생성기를 고쳤다가 되돌렸다. PAPER §7 에 «일곱 번째는 감사 도구 자신»으로 등재.

## 2026-08-29 — 시드 보강(n=5)과 세 건의 철회

**확정 수치 (동일 장비·동일 정숙 조건, 각 5시드)**
- 경량 전이 **49.3% ± 16.1%** (27/67/53/40/60) vs 네이티브 **62.0% ± 14.5%** (47/67/77/73/47)
- 차이 −12.7%p, Welch t=−1.31, df=7.9, **p=0.227**, 95% CI [−35.0, +9.7] — **유의하지 않다**
- 5분 예산에서는 경량이 **+18.0%p** 앞선다(25개 시드쌍 중 22쌍). ⚠ 미보정 p=0.039 는
  12개 체크포인트 중 **최소 p** 라 다중비교를 통과하지 못한다(Bonferroni 0.47, 순열 max-T
  0.26). 유의한 것은 개별 시점이 아니라 **교차 구조**다 — 위 2026-09-02 항 참조
- 15분에 교차. 실현 처리량 35.9배. 오라클 조기정지 64.7% vs 완주 49.3%
- 교차의 원인은 경량의 하강이 아니라 **네이티브의 상승** — 경량 곡선은 첫 체크포인트부터 평탄

**n=3 → n=5 에서 철회된 것 (전부 소표본 아티팩트)**
| 주장 | n=3 | n=5 |
|---|---|---|
| 5분→40분 하강 | t=17.0, p=0.0034 (Bonferroni 후에도 0.038) | t=0.5, p=0.671 |
| 실패가 충돌→이탈로 이동 | 3/3 양, Fisher p=0.0066 | 4/5 양, 부호 불일치 |
| DPC 신뢰 창 구조 | +0.457→+0.053 (p=0.029) | +0.190→+0.039 (p=0.42) |

**살아남은 것**: 탐색 붕괴(엔트로피·소스수익·에피소드길이·approx_kl 전부 5/5, p<0.0001,
행동 σ 0.216→0.045). 다만 이것이 전이로 번역되지 않는다 — 그 비대칭 자체가 결과다.

**함정 두 개 기록**
- **Fisher 결합 p 는 부호를 보지 않는다.** 이탈 추세는 n=5 에서 부호가 갈렸는데도 결합
  p=0.0027 이었다. 부호 불일치 시 결합 p 인용 금지(도구에 경고 추가).
- **다중비교 보정은 분산 과소추정을 못 잡는다.** 5분→40분 낙폭은 Bonferroni 를 통과했지만
  n=3 의 산물이었다.

## 2026-08-29 — 장비 교락 해소

헤드라인 비교가 **장비 교차**였다(경량=데스크톱, 기준선=노트북). 같은 데스크톱의 단일 시드
네이티브가 53.3% 라 격차가 4.4%p 로 좁혀지는 듯했으나, 그 실행이 GPU 경합 구간이었다
(602 SPS vs 정숙 1,096 SPS, 표본 2.17M vs 3.95M). 정숙 조건 5시드로 다시 재 62.0% 확정.
실현 처리량도 62.6배(경합 분모) → **35.9배**로 정정. 근거: docs/HARDWARE_CONFOUND.md

## 2026-08-29 — UE 장면 생성기 감사 (43건 → 21건 검증 통과, 18건 반영)

- **좌측통행이었다**: 출하 464장면 4,323 라벨에서 순방향의 88.6% 가 화면 왼쪽. 평가 도메인
  (Udacity CrowdAI, 캘리포니아)은 우측통행이라 학습·평가가 거울상 — §6.4 잔여 갭의 제3 요인
- **완전 가림 라벨 20.7~22.8%** 에 온전한 GT (장면의 76~80% 에 1건 이상) → 가시비 라벨 도입
- **건물 상호관통 87.7%** (측면), 타워 71.2%, 타워 도로 침범 92.8% → 예약 배치로 전부 0%
- 인도 위 액터 15 cm 매몰(장면당 26.7개), 차로 이산화 부재, bbox2d 근평면 폐기, 카메라
  자유도 0(지평선 행 분산 정확히 0), `level` 필드 464/464 오염 등
- **검증 렌더에서 회귀 2건 추가 발견**: 타워를 도로 밖으로 밀었더니 지면이 없어 허공에 떴고,
  차량 x 범위를 2.6배 넓히면서 대수를 안 올려 근경이 비었다 → 원경 지면·N_VEH_RANGE 수정

**좌표 규약 오류 누적 6건** (§7 표). 6건 중 4건이 "축이 정렬된 구간에서 항등사상"이라
축정렬 테스트만으로는 구조적으로 통과한다.

## 2026-08-28 — 부호 오류 발견과 논문 재구성


**사건**: 경량 시뮬의 횡방향 좌표가 MetaDrive 규약과 거울상이었다(MetaDrive 는 차선 API=우+,
차량 API=좌+ 두 규약을 동시에 사용). 전이 평가에서만 좌우가 반전돼 성공률 0%·이탈 95% 가
나왔고, 이를 "과최적화 착취"로 오독해 §6.3·§6.5·DPC 를 그 위에 세웠다.

**정정 결과 (브랜치 fix-lateral-sign)**
- 경량 전이: IQM 0% → **48.9%±20.4%** (정숙 조건 136M, 3시드 확정) vs 네이티브 68%±9% — 열세이나 유의하지 않음(Welch p≈0.24). GPU 경합 20M 조건에서는 67.8%±19.0%. ⚠ 기준선은 노트북, 확정치는 데스크톱 — 장비 교차 비교(동일 장비 네이티브 다시드 진행 중)
- 붕괴 소멸: 전 구간 50~95% 유지, 이탈 0~20%
- 대조 실험: 네이티브 정책에 같은 보정 적용 시 55%→0% (규약 불일치 확증)
- DPC 재산출: 붕괴형 감소 소멸, 전 구간 τ≈0 (예측력 부재)
- §5 재측정: V=2 전이 5% → **마스킹 구제 55%** (기존 13.5%)

**논문 재구성**
- 신설 §4.7: 부호 오류의 위장 기제 + 판별 프로토콜(역방향 대조·필드 단위 기하 왕복·회전/직진 분해)
- 신설 §5.4: CARLA 폐루프 지지집합 경계 — 슬롯 3=93%, 4=0% 절벽. 양방향 대칭(부족/과다)
- 폐기: §6.3 착취, §6.5 처방 3종 판정, DPC 붕괴 주장
- 초록·§8 재작성, 그림 재생성(fig_sign_error, fig_support_boundary, fig_carla_ablation)

## CARLA 폐루프 주행 (제3 시험장)

- 관측 어댑터(51차원 GT)+행동 매핑+판정 구현, 정책은 numpy 로 실행
- 성공률: 전체 95%, 우회전 96%, 좌회전 75~100%, 직진 100% (거버너 0.8g, 슬롯 3)
- 절제(셀당 n=120): 거버너 ON 94~95% vs OFF 63~70% — 거버너가 지배적(+31pp)
- 정합 함정 9종 문서화: docs/CARLA_CLOSED_LOOP.md
- 시각화: 8카메라 테슬라 리그, 4레인 분할 대시보드(GT 대조), 재생 GIF

> **(2026-09-05 병합 충돌 해소)** 이 자리에 «2026-08-24 핵심 사건: V=3 전이 0% 붕괴»
> 블록이 미해결 충돌 마커와 함께 3주째 남아 있었다. 그 내용은 **전부 부호 규약 오류의
> 산물로 철회됐다**(아래 2026-08-28 항, PAPER §4.7) — 붕괴도, 착취 해석도, 그 위에 세운
> §6.3 도 존재하지 않는다. 충돌의 양쪽 모두 현재 유효하지 않으므로 삭제하고 이 주석으로
> 대체한다. 이력은 커밋 63c7ee1 이전에 남아 있다.

## 전체 목표

가벼운(경량) 자체 시뮬레이터로 자율주행 강화학습 환경을 만들고,
**동일 wall-clock 예산**에서 고충실도 시뮬레이터(MetaDrive)와 비교하는 논문을 쓴다.
논문 본문: PAPER.md (v2, 실측 이력: docs/PAPER_worklog_v1.md)

## 지금 어디에 있나 (2026-08-24 저녁)

- **노트북**: 배치4 완료(15:24) — 주 결과 반전(위 핵심 사건). **배치5(3h 예산확장) 진행 중.**
- **데스크톱(이 장비)**: 윈도우 재현 환경 구축·검증 완료. 16:05 발사했던 자체 배치는
  역할 분담 확정으로 즉시 중단·폐기(산출물 없음). 이후 **언리얼 전용**.

### ⚠ 코드-데이터 일관성 (다음 RL 실험 전 필독)

노트북의 본판 데이터셋(MD 66%, V=2, V=3 반전 결과)은 **ppo.py 수정 6 이전 코드**로
측정됐다. main 최신에는 수정 6(인덱스 전송·fused Adam·예산 시계 위치)이 들어가 학습
처리량과 수치 궤적이 달라진다. 따라서:
- 처방 실험((ii) 경계 보수화, (iii) 캐스케이드)을 기존 데이터셋과 비교하려면 노트북은
  **78777d2 시점의 bench/ 코드**로 돌려야 한다 (pull 후 `git checkout 78777d2 -- bench/ppo.py`).
- 수정 6 코드로 갈아타려면 전 조건 재측정(run_all.sh)이 필요하다. **혼용 금지.**

## 이 장비 환경 (구축 완료, 검증됨)

| 항목 | 값 | 비고 |
|---|---|---|
| Python | 3.11.9 | metadrive 0.4.3 의 requires_python 이 `<3.12` |
| torch | 2.9.1+cu128 | arch_list 에 sm_120 포함, capability (12,0) 실측 |
| metadrive / gymnasium / panda3d | 0.4.3 / 1.2.3 / 1.10.13 | 코드 주석이 못 박은 버전 |
| numpy / numba | 2.4.6 / 0.67.0 | |

검증 통과:
- `bench/test_env.py` T1~T5 전부 통과. 완성 환경 SPS 969,757 (노트북 1,002,242 — 사실상 동일)
- `bench/test_equivalence.py` 세 구현 동치 (obs 최대차 2.4e-07, reward 차 0, done 불일치 0)
- MetaDrive 12워커 AsyncVectorEnv 가 윈도우 `spawn` 에서 정상 기동 (워커 12 + 메인 1)

## 윈도우 이식에서 실제로 고친 것

1. **cp949 인코딩** — 한국어 윈도우 콘솔이 유니코드 출력에서 죽는다 → `PYTHONUTF8=1` (bench/env.sh)
2. **경로/인터프리터 하드코딩** — run_main*.sh 5벌이 각자 `/home/karma/thesis` + `.venv/bin/python`
   → OS 분기를 `bench/env.sh` 한 곳으로. 본판 전체는 `bench/run_all.sh` 하나로 통합
3. **`.sh` 개행** — `.gitattributes` 로 LF 고정 (CRLF 가 붙으면 bash 가 파싱 실패)
4. **`n_vehicles` 기본값 함정** — f8aa92c 에서 2→3 으로 바뀌었고 원 run_main.sh 는 기본값에
   의존했다. 그대로 돌리면 V=2 병리 표본이 조용히 V=3 이 된다 → run_all.sh 는 조건마다 명시
5. **벽시계 측정 오염 (ppo.py 수정 6)** — 아래

### ppo.py 수정 6: 벽시계 측정 오염 제거

수식·난수·미니배치 구성은 불변이고 데이터 이동과 커널 선택만 바꾼다.

- (a) 미니배치 인덱스를 에폭당 1회만 GPU 로. 원본은 numpy 인덱스로 CUDA 텐서 6개를
  매 스텝 색인해 스텝마다 호스트→디바이스 전송과 동기화를 일으킨다.
- (b) Adam 을 fused 커널로. 기본 Adam 은 bias correction 에서 파라미터마다 `step.item()`
  을 불러 스텝당 26회 디바이스 동기화가 발생한다.

원인 실측(이 장비): `.item()` 1회 **72.5 us** vs 비동기 커널 11.1 us — 윈도우 WDDM 은
디바이스 동기화가 리눅스보다 한 자릿수 비싸다. 노트북(리눅스)에서 72K SPS 가 나온 코드가
이 장비에서 19K 로 떨어진 원인이 이것이다. 즉 이 수정은 경량 시뮬에 유리한 조작이 아니라
**플랫폼 아티팩트 제거**다.

| | 수정 전 | 수정 후 |
|---|---:|---:|
| 경량 V=3 학습포함 SPS | 22,700 | **43,480** |
| MetaDrive 학습포함 SPS | 905 | **1,076** |
| 격차 | 25× | **40×** (노트북 53×) |

두 시뮬이 같은 ppo.py 를 쓰므로 비교는 공정하다. 논문에 수정 항목으로 명시할 것.

## 역할 분담 (2026-08-24 저녁, 사용자 지시)

- **데스크톱 (RTX 5080 / Windows, 이 저장소) = 언리얼 전용**
- **노트북 (RTX 4060 Laptop / Linux, /home/karma/thesis) = 강화학습 전용**

데스크톱에서 16:05 발사했던 본배치는 역할 변경으로 즉시 중단·폐기 (산출물 없음).
원격: 데스크톱 ↔ 노트북은 Tailscale 로 연결됨 (노트북 = karma / 100.126.163.103).

## 처방(ii) 경계 보수화 — 기각 (2026-08-25 00:50 실측)

δ=0.3 x2시드·δ=0.5 x1시드 (exp-boundary 브랜치 = 78777d2+수축 패치): 전 시드 최종 0%
붕괴 재현(붕괴 ~25분, 대조군 동일), in-domain 78~88% 정상. δ=0.5m 도 무력 → 착취 대상은
공간 경계 잔차가 아님. 남는 처방 = 조기정지·(iii) 캐스케이드. PAPER §7 반영 [리뷰 대기].
원자료: 노트북 bench_results/exp_boundary/, 브랜치 origin/exp-boundary.

## 번호판 복원 v2 + 그림 3종 (2026-08-28 새벽)

- 진실 정정: UE 번호판은 원래 진짜 텍스트 17종(UDIM 타일) — 의사문자는 디퓨전 산물. "6MT776 성공"은 거짓 양성(레이어 파라미터 연관 함정)
- 해법 = 흰-패스 마스크(gain>550 + 형상 필터) → fin_plain 픽셀 되붙임 → **fin_refined_v2** (291장 복원, 반사 오검은 형상 필터로 제거)
- 논문 그림: figs/fig_4arm.png, fig_ft.png, fig_dpc.png (한글 폰트·rliable 문법)
- few-shot 파인튜닝 §6.4 반영: 25장 구간 합성 사전학습 +47.7pp·분산 1/32
- union_ds(depth+seg, SAM색) 소표본 -4.5% 우세 → 360장 전체 검증 진행 중 (out_g6full)

## percept v1 4-arm 완료 (2026-08-26 01:50) — 인지 트랙 핵심 실험 완결

- Udacity 실사 532프레임 mAP50: raw 46.5 / plain 45.2 / **refined 53.7** / real 상한 89.7
- 결론: 디퓨전 정제 +7.2pp(P·R 동반 상승 = 표현 개선), 센서시뮬 단독 무익(평가 도메인 조건부), 잔여 갭 36pp(장면구성+규모)
- PAPER §6.4 반영 [사용자 리뷰 대기], 갤러리에 표 추가. 원자료 C:/ue/percept/(runs, results_v1.json)
- RL 트랙과 대구: 행동 갭은 처방 무력 / 외형 갭은 정제 유효 — 논문 종합 결론 성립

## 프로덕션 데이터셋 완성 (2026-08-26 새벽)

- 360장 렌더(scene_200~559) + bbox JSON 360 — 이중 실행 사고(상호 taskkill)는 잠금장치로 봉인
- 프롬프트 A/B: mundane(미적어 제거) 가설 **지표 기각**(sKVD .280 vs .263, KDD/CMMD도 열세) → default 채택
- 정제 파이프라인: 색정합→깊이CN 디퓨전 0.3→ISP, 2팔 산출 `C:/ue/out_prod/fin_refined|fin_plain`(각 360)
- 색기움 전수 감사: 19장(>28) 검출 → 저강도 0.25 재처리(최악 54→37, 잔여는 날씨 프리셋 기인 수용)
- sKVD 지표 가동(ue/skvd_metric.py, SegFormer+VGG 패치 MMD) · 참조 실사 170장 체제(ref_all)
- Udacity CrowdAI 라벨셋 확보(MIT, 1,570프레임/11,970박스) `C:/ue/real_labeled/`
- 갤러리(같은 URL): https://claude.ai/code/artifact/f9f74705-39ee-4afa-becb-e13f6e1aaac5
- 다음: percept v1 2D 차량 검출 4-arm(UE bbox→YOLO 변환, Udacity 평가 — 도메인 갭은 상대비교 통제)

## 처방 실험 최종 결산 (2026-08-25 오후) — RL 트랙 실험 완결

- (ii) 경계 보수화: **기각** (δ0.3/0.5 전 시드 붕괴 재현)
- (iii) 캐스케이드: **기각** (39%±21% vs 네이티브 66%±10% — s1만 63% 동급, s2 붕괴·s3 정체)
- (i) 조기정지만 생존. 논문 결론 = "경량 시뮬 가치는 대체·가속이 아니라 진단" [§7 리뷰 대기]
- 원자료: 노트북 exp_boundary/exp_cascade, 브랜치 origin/exp-boundary·exp-cascade

## (구) 캐스케이드 발사 준비 기록

패치(--init-ckpt: 가중치+obs_rms 웜스타트) 작성·앵커 검증 완료 (78777d2 기준).
설계: 배치4 final_custom 의 t000600.pt(10분, 피크 부근)를 초기값으로 MD 50분 미세조정
x3시드 → 합계 60분 벽시계 vs 네이티브 60분(66%). 소요 ~2.5h+평가. "ㄱ" 시 즉시 발사.

## 노트북 원격 조종 (SSH 연결 완료)

Tailscale 연결 확인(karma / 100.126.163.103, ping 93ms). SSH 키 인증만 남음 —
데스크톱 공개키를 노트북 `~/.ssh/authorized_keys` 에 1회 등록하면 이후 데스크톱 세션이
노트북 GPU 작업(처방 실험·평가·모니터링)을 직접 수행한다.
배치5가 도는 동안은 읽기 전용 모니터링만 (경합 금지 원칙 동일).

## 언리얼 / 인지 트랙 — 장면 생성 파이프라인 v2 가동 (2026-08-24 저녁)

**"실제같은 사진" 파이프라인 완성**: ue/scene_build_cs.py(생성, 커맨드릿) +
ue/cs_render.sh + ue/shot_once.py(렌더, -game). 도시 협곡 장면 — 도로 키트 아스팔트,
인도, 점등 가로등, 히어로 빌딩 양측, 차량 3~8대(도색 랜덤·겹침 배제·접지), 태양/안개
장면별 랜덤, 라벨 = 3D 상대위치·요각·크기 + bbox2d(핀홀 투영).

검증 세트 4세대 (scene_160~169, C:/ue/out_nr/gen4/): **신경망 재렌더링 도입** —
UE 렌더 → 실사 색정합 → 디퓨전(RealVisXL+ControlNet-Depth, strength 0.4) → 센서 노이즈.
strength 스윕 실측: 0.3/0.4 기하·bbox 보존, 0.5 드리프트. 파이프라인 순서 3변형 대조 후
C(정합→디퓨전→노이즈) 채택 (KDD 최고 + 노이즈 균일성). 컨택트 섀도로 접지 해결.
지표: 신경정제에서 KID 0.061→0.039(−37%)·CMMD 3.04→2.07(−32%). 판정단 최종 라운드에서
사상 첫 "수초 고민" 발생. scene_163 은 첫 실사 오인 후보.
장당 비용: 렌더 75s + 디퓨전 ~15s. ue/neural_refine.py 신규.
**사용자 육안 판정 대기** — 합격 시 대량 생성 진행 (HARNESS 규칙).
추가 실측 함정: 특정 레벨명(gen_0/gen_5)이 구성 무관 D3D12 페이탈 재현 — 신선한
인덱스 대역으로 우회(빌더 독스트링 기록).

하루 동안 실측으로 확정한 함정 (전부 코드 주석·메모리에 등재):
- 커맨드릿은 렌더 불가(하늘만) → 생성/렌더 프로세스 분리
- -game 에서 에디터 API 는 크래시, 월드는 find_object, 파일명은 HighResShot filename=
- LevelInstance 는 커맨드릿에서 비동기 미로드 — 바운드 사용 금지, 실측 반폭 테이블로 배치
- GNU timeout 이 UE 윈도우 자식을 못 죽여 고아가 VRAM 14GB 점유 → 전 부팅 D3D12 사망
  (렌더마다 taskkill 청소로 해결)
- 특정 장면 구성이 HighResShot 시점 D3D12 행 유발 (~2/10) → 시드 교체 재생성으로 회피

다음 개선 후보: 교차로/횡단보도 타일 믹스, 가로수·소품 밀도, 카메라 포즈 랜덤화,
percept v1 학습 연결(bbox2d), CARLA 데이터와 혼합.

## ✅ PAPER.md §6.4 지면평면 σ 정정 — **반영 완료** (PAPER.md 860~870행)

`bench/percept_v0.py` 의 "지면평면 거리추정"이 **대수적으로 항등 소거된다.**
`box_corners` 가 하단 모서리를 정확히 `z=-h_cam` 에 놓고 `h_cam` 을 라벨에서 역산하므로
`FX=FY` 조건에서 `d_gp = FX·h_cam/(v1−CY) = x_min` (최근접 바닥 모서리의 종방향 거리).

CARLA 500프레임 1,000건 실측:

```
|d_gp − x_min|                     최대 3.6e-14   ← 부동소수점 오차 수준
오차(d_gp − gt_x) 부호              음수 100.0%
|오차| vs 예측 오프셋 l/2·|cos|+w/2·|sin|   최대차 3.3e-14
 4-15m  σ=0.84m  평균오차 −3.04m
15-30m  σ=1.10m  평균오차 −2.53m
30-50m  σ=1.17m  평균오차 −2.48m
```

σ 0.84/1.10/1.17 은 PAPER.md 게재값과 정확히 일치한다. 그러나 이 σ 는 평면 가정이나
캘리브레이션 오차가 아니라 **"박스 중심 vs 최근접 모서리"라는 정의 차이에서 오는 결정론적
기하 오프셋의 산포**다 (평균오차가 −2.5~−3.0m 로 전부 한쪽으로 쏠린 것이 증거).
B4 관측 노이즈 주입의 σ 로 쓰면 랜덤 노이즈가 아닌 것을 노이즈로 넣는 해석 오류가 된다.

**처리 결과 (2026-09-05 확인)**: 첫 번째 방향으로 반영됐다 — PAPER.md §6.4 에
「정정 (2026-08-24 재검증, 확정)」 문단이 이미 들어가 있고, σ 를 노이즈 주입에 그대로 쓰면
해석 오류가 된다는 경고와 재측정 조건 (a)·(b) 까지 적혀 있다. 이 STATUS 항목이 3주 동안
«정정 필요» 로 남아 있었던 것은 문서 지연이지 미반영이 아니다.

## 확정된 본판 수치 (노트북 4060/Linux, ppo.py 수정 6 이전 코드 — 논문 §6.2 확정본)

- MetaDrive 네이티브: 66% ± 10% (63/57/77)
- 경량 V=2 (지지집합 결함): 5% ± 7% (0/10)
- 경량 V=2 + 퇴화차원 마스킹: 13.5% ± 5% (10/17)
- 역방향(MD정책→경량 V=3): 2% ± 3%
- 동일 시뮬 소거 실험: V=2 정책을 V=2 평가 81/89% ↔ V=3 평가 22/36%
- 처리량: 경량 1.0M SPS vs MetaDrive 12proc 6,965 SPS = 138×

## 보류

- bottleneck 일반화
- 노이즈 주입 실험 — **사전 감도 측정으로 「이 설계로는 성립하지 않는다」가 확정됐다
  (2026-09-16)**. 실험 자체는 여전히 미착수이나, 돌리기 전에 답이 나왔다.
  주입값(σ 1.10/2.82/5.10 m, 편향 −0.13/−0.55/−1.72 m)과 주입 장치(`bench/obs_noise.py`)는
  준비돼 있고 실측 분포 재현·불변식·포화 0.00% 까지 검증했다. 그 위에서 **재학습 없이**
  보관 체크포인트로 잰 것이 `bench/noise_sensitivity.py` (결과 `bench_results/noise_sensitivity/`):
  - (A) 실측 크기 주입은 |Δ조향| 0.0125 — 정책 자신의 탐색 σ(0.046)의 **0.27배**,
    스텝간 행동 변화(0.335)의 0.04배.
  - (C) 이 비율은 학습 **전 구간**에서 0.03(t=300s, σ=0.324)→0.27(final) 로 **한 번도
    1 을 넘지 않는다**. 「수렴한 정책만 둔감한 것 아니냐」는 반론이 닫힌다.
  - (B) 주입 관측을 정책이 실제로 받아도 성공률 81.2%→80.8%(p=0.75), **실측 4배**에서도
    −0.2%p. 종방향 거리 오차는 이 과제에서 성능과 무관한 입력이다.
  ★ **어제 적은 점유율 7.4% 는 무작위 행동으로 잰 값이었다** — 학습된 정책으로 굴리면
  20.4% 다. 정책은 도로를 따라 달려 이웃 차량 곁에 머무르기 때문이다. 설계에 쓸 값은 후자.
  ★ 그럼에도 결론은 같다: 닿는 면적이 20% 여도 섭동이 탐색 잡음 이하라 경로를 못 바꾼다.
  **승인을 구할 것이 남아 있다면** 실험 자체가 아니라 그 설계 변경이다 — (a) V 상향,
  (b) §6.4 가 재지 않은 차원(횡방향·요각·속도)의 인지 오차 측정, (c) 배율 확대(단 (c) 는
  질문을 「실제 인지 오차」에서 「큰 합성 잡음」으로 바꾼다). PAPER §8 (9) 에 반영 완료.
  **학습 경로에는 여전히 배선하지 않았다** — 배선 지점은 커널이 obs 를 채운 직후 한 곳.
- 인지 트랙 4-arm 재실행 — 통행 방향(좌→우측) 정정판 생성기로. PAPER §8 향후 연구 (5-c).
  **UE 검증 렌더 육안 합격이 선행 조건**이다.
- ~~평가 시나리오 축 복원~~ — **완료(2026-09-11, 블록 8개로 확장 2026-09-16)**. §7 표 6, 8/8 음수 p=0.0078. 난이도 출처까지 측정 완료.
